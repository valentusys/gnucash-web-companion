# Phase 28 — GnuCash Compatibility Matrix

## Status

Complete.

## Goal

Document and test the current GnuCash SQL compatibility baseline using committed synthetic fixtures, while avoiding broad compatibility claims that are not yet proven.

## Scope

- Documentation and tests only.
- No production code changes.
- No new real GnuCash books.
- No multi-version fixture generation from real user data.

## Changes

### docs/gnucash-compatibility.md

- Added current tested matrix for:
  - `apps/api/tests/fixtures/test-book.gnucash.sqlite`
  - `apps/api/tests/fixtures/test-book-multicurrency.gnucash.sqlite`
- Documented SQL `versions` markers:
  - `Gnucash = 3000000`
  - `Gnucash-Resave = 19920`
- Documented untested areas:
  - PostgreSQL/MySQL/MariaDB GnuCash backends
  - XML books
  - fixture books generated by multiple real GnuCash Desktop versions
- Added safe user guidance for inspecting copied SQLite books without leaking real financial data.

### apps/api/tests/test_gnucash_compatibility.py

- Added tests verifying committed synthetic fixture schema markers.
- Added tests verifying the compatibility document names the fixtures and key limitations.

### README.md

- Added link to `docs/gnucash-compatibility.md` under compatibility/safety docs.

### PROJECT_STATUS.md

- Advanced baseline through Phase 28.
- Added Phase 28 summary.

## Safety checks

- `GNUCASH_WRITES_ENABLED=false` remains the documented default.
- No production code or write-path changes.
- No real financial data, real GnuCash books, backups, app DBs, secrets, keys, certs, or real screenshots added.
- Compatibility language remains conservative and does not claim support for untested backends/versions.

## Verification

- Backend: `cd apps/api && pytest -q` — 264 passed, 27 warnings.
- Frontend: `cd apps/web && npm run check && npm run test:auth-routes && npm run build` — OK.
- Docker config: `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet` — OK.

## Commit

Pending until commit/push step completes.

## GitHub

- Related issue: #15 GnuCash version compatibility matrix.
- Expected state after commit: issue #15 can be closed as completed for the initial documented/tested compatibility baseline, with future multi-version fixture work left for later if needed.

## Blockers

None.
