# Independent Audit — 2026-05-18 — Phase 51

## Executive summary

Phase 48–50 work preserves the read-only MVP boundary. UX copy, transaction filters/export, and book switching are documented and tested as read-only behavior. Multi-book UI is framed as multiple independent books with scoped access, not shared-wallet or collaborative accounting. No blocker was found.

## Verdict

Ready for pre-alpha release.

## Top blockers

None.

## Important non-blockers

1. GitHub issues #11 and #13 remain intentionally open for broader future search/filter and admin-only book-management work; Phase 49/50 completed only stabilization subsets.
2. CSV exports can contain sensitive financial data by design; docs warn users not to commit real exports.
3. Existing committed `.gnucash.sqlite` files are historical synthetic test fixtures; no new generated compatibility fixture binary was committed by Phase 46–50.

## Product consistency

- README keeps the project positioned as pre-alpha, read-only-first, self-hosted, not SaaS, not a GnuCash replacement, and not true collaborative multi-user accounting.
- README explicitly excludes family shared-wallet baseline and safe write-mode access to an only copy of a book.
- Phase 50 book-switcher copy says `independent read-only books` and docs say multi-book support means independent books with scoped access.
- No inspected docs or UI copy from Phase 48–50 reframe the app as a family wallet or collaborative accounting app.

## Safety boundary

- `.env.example` keeps `GNUCASH_WRITES_ENABLED=false`.
- README says controlled writes are experimental post-MVP and disabled by default.
- Phase 48–50 changes did not expand write routes or enable write mode by default.
- `apps/web/scripts/test-auth-routes.mjs` includes static checks for disabled write UI gating, write acknowledgement, filter URL/export parity, active-book fallback, route usage, and no upload/collaborative/family-wallet framing in the book switcher.
- Tracked sensitive-name scan found only the two historical synthetic test fixtures:
  - `apps/api/tests/fixtures/test-book.gnucash.sqlite`
  - `apps/api/tests/fixtures/test-book-multicurrency.gnucash.sqlite`

## Release/readme/docs consistency

- README current status was still through Phase 50 before the Phase 51 engineer sync, which is expected at audit time.
- Transaction filter behavior is documented in `docs/transactions-filters.md`, including validation, URL/pagination behavior, CSV export parity, cap, and sensitive-export warning.
- Book switcher behavior is documented in `docs/book-switcher-readonly-model.md`, including access boundary, fallback order, and explicit non-goals.
- `PROJECT_STATUS.md` correctly showed Phase 51 as next planned phase before this audit.
- `CHANGELOG.md` includes Phase 48–50 user-facing stabilization entries before this audit.

## GitHub project hygiene

Open issues relevant to this audit:

- #11 — Transaction search/filter improvements: Phase 49 hardened current filter behavior; broader improvements remain open.
- #13 — Book management UI: Phase 50 stabilized the switcher only; admin-only book management remains open.
- #17, #22, #12 remain future roadmap items and do not block Phase 51.

No new GitHub issue is recommended from this audit because no blocker or new material mismatch was found.

## Security notes

- Auth storage remains cookie-based in the inspected frontend static checks; no localStorage/sessionStorage auth path was found in the route-check script.
- The selected book cookie is non-secret and server-side page loads resolve it against the accessible `/books` list before constructing book-aware routes.
- CSV export is read-only but can expose sensitive financial data to the user; docs warn against committing real exports.
- No production security or audited-security claim was introduced.

## Test/CI notes

Audit evidence inspected:

- `AGENTS.md`
- `PROJECT_STATUS.md`
- `CHANGELOG.md`
- `README.md`
- `docs/handoff/phase-48.md`
- `docs/handoff/phase-49.md`
- `docs/handoff/phase-50.md`
- `docs/transactions-filters.md`
- `docs/book-switcher-readonly-model.md`
- `apps/web/src/lib/api/server.ts`
- `apps/web/src/lib/components/BookSwitcher.svelte`
- `apps/web/scripts/test-auth-routes.mjs`
- `apps/api/app/routers/transactions.py`
- `.env.example`

Required Phase 51 checks should still be run by the engineer after accepted documentation/status sync:

- `cd apps/api && pytest -q`
- `cd apps/web && npm run check && npm run test:auth-routes && npm run build`
- `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet`
- `git diff --check`

## Recommended next actions

1. Accept no code blockers from this audit.
2. Engineer should perform documentation/status sync for Phase 51 only: README latest-audit link, `PROJECT_STATUS.md`, `CHANGELOG.md`, and `docs/handoff/phase-51.md`.
3. Update GitHub issues #11 and #13 with the Phase 51 audit result if `gh` is available.
4. Proceed to Phase 52 only in a separate phase after commit/push and clean tree.

## Suggested GitHub issues

None.

## What not to do next

- Do not expand controlled-write scope.
- Do not add book upload/import or admin book-management UI as part of this audit phase.
- Do not start Russian localization/i18n work in Phase 51.
- Do not publish a release/tag from this phase.
- Do not commit real books, real exports, `.env`, app DBs, backups, secrets, keys, tokens, or real screenshots.
