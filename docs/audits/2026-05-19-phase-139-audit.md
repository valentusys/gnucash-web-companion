# Phase 140 audit — v0.1.4-readonly maintenance readiness

Date: 2026-05-19
Audited HEAD: `3f2fb2c6d3b31d339d0dc560e86012865a7467a4`
Branch: `main`
Scope source: roadmap Phase 8 / project Phase 140

## Verdict

`not ready`

The release-readiness gate is not ready for `v0.1.4-readonly` artifacts/publication yet because public status documentation is not fully synchronized: `README.md` still says Phase 0–137 are complete and lists recent post-release maintenance only through Phase 137, while `CHANGELOG.md` `Unreleased` records Phase 137 and earlier but not Phase 138/139. Local checks, write-gating, Docker config, sensitive-file hygiene, and recent GitHub CI passed.

This is not a product-code blocker and not a safety breach. It is a release-documentation synchronization blocker to resolve before a maintenance release can be represented honestly.

## Checks performed

| Check | Evidence | Result |
|---|---|---|
| Initial git state | `git status --short`, `git rev-parse HEAD`, `git branch --show-current`, `git log --oneline -5` | PASS with only pre-existing untracked `.hermes/`; branch `main`; starting HEAD `3f2fb2c6d3b31d339d0dc560e86012865a7467a4`. |
| Required context read | `AGENTS.md`, `PROJECT_STATUS.md`, latest `docs/handoff/phase-139.md`, roadmap Phase 8 section | PASS. |
| Roadmap phases 1–3 documented/tested | `docs/handoff/phase-133.md`, `docs/handoff/phase-134.md`, `docs/handoff/phase-135.md`; frontend route checks and `npm run check` recorded in each handoff | PASS. Phase 133 records frontend/auth-route/backend verification; Phases 134/135 record RED/GREEN `npm run test:auth-routes`, `npm run check`, Docker config, diff/sensitive scans. |
| `GNUCASH_WRITES_ENABLED=false` remains default | `apps/api/app/config.py`, `.env.example`, `docker-compose.yml` | PASS. Backend settings default is `False`; `.env.example` says `GNUCASH_WRITES_ENABLED=false`; Compose defaults API and web to `${GNUCASH_WRITES_ENABLED:-false}`. |
| Write-gating regression tests | `cd apps/api && pytest tests/test_transaction_writes.py::TestWritesDisabledByDefault -q` | PASS: `8 passed, 1 warning`. |
| Frontend route/auth checks | `cd apps/web && npm run test:auth-routes` | PASS: `auth route checks passed`. |
| Docker Compose config | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet` | PASS. |
| GitHub CI | `gh run list --limit 5`; latest run was watched with `gh run watch 26083287539 --exit-status` | PASS. Latest five `main` CI runs are `completed success`; latest Phase 139 run `26083287539` passed backend, frontend, foundation, and Docker validation jobs. |
| Sensitive tracked-file scan | `git ls-files` patterns for `.env`, `app.db`, GnuCash/SQLite DBs, backups, secrets, keys/certs | PASS with caveat. Only tracked DB-like files are intentional test fixtures `apps/api/tests/fixtures/test-book*.gnucash.sqlite` plus `data/app/.gitkeep`; ignored runtime `data/app/app.db` and `data/books/` exist locally but are not tracked. |
| README/CHANGELOG/PROJECT_STATUS synchronization | Read `README.md`, `CHANGELOG.md`, `PROJECT_STATUS.md` | FAIL. `PROJECT_STATUS.md` is current through Phase 139, but `README.md` still says Phase 0–137 are complete and recent post-release maintenance through Phase 137; `CHANGELOG.md` `Unreleased` lacks Phase 138/139 entries. |

## Release readiness notes

- Ready aspects:
  - Write disabled-by-default posture is intact.
  - Write-gating tests pass.
  - Docker Compose config validates with dummy secrets.
  - Recent GitHub CI is green.
  - No tracked real book, app DB, backup, `.env`, token, key, cert, screenshot, or private export was found in this audit pass.
  - Roadmap phases 1–3 have documented handoffs and regression checks.
- Not-ready aspect:
  - Public docs are not fully synchronized with the latest completed phase state. Do not prepare/publish `v0.1.4-readonly` until README/CHANGELOG/release artifacts honestly include the Phase 138 status sync and Phase 139 synthetic dogfood evidence, or explicitly explain the intended release scope.

## Safety statement

This audit changed no product code, did not enable or expand write-alpha behavior, did not create a tag, did not publish a GitHub release, and did not add real/private data. The repository remains pre-alpha, read-only by default, test-copies-first, not production-ready, and not security-audited.
