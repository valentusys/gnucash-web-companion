# Independent Audit — 2026-05-18

Scope: Phase 47 auditor pass after Phase 46 compatibility fixture work.

## Executive summary

The Phase 46 compatibility fixture work is safe enough for the next read-only roadmap phase. The generated fixture path uses synthetic disposable data, writes generated SQLite output to an ignored local directory by default, and documents that it is `piecash`-generated rather than a broad GnuCash Desktop compatibility matrix. No blocker was found.

## Verdict

Ready for pre-alpha release.

No release/tag is requested or performed in Phase 47; this verdict means the compatibility fixture work does not block continuing the read-only pre-alpha roadmap.

## Top blockers

None.

## Important non-blockers

1. Compatibility coverage remains limited: Phase 46 validates a generated `piecash` SQLite fixture, not books saved by multiple GnuCash Desktop releases.
2. GitHub issue #22 should remain open unless maintainers explicitly decide the generated fixture path is enough; desktop-version fixtures are still future work.
3. The repository still contains two historical committed synthetic SQLite fixtures under `apps/api/tests/fixtures/`; they are expected test fixtures, not new Phase 46/47 generated binaries.

## Product consistency

Consistent.

- `README.md` says the project is pre-alpha, read-only-first, self-hosted, not a GnuCash replacement, not SaaS, not collaborative accounting, and not production/security-audited.
- `docs/gnucash-compatibility.md` describes the generated fixture row conservatively and says desktop-version fixture books are not yet formally tested.
- `docs/gnucash-compatibility-fixture-v1.md` explicitly says the fixture is synthetic, disposable, `piecash`-generated, and not broad GnuCash Desktop compatibility coverage.

## Safety boundary

No safety regression found.

- `GNUCASH_WRITES_ENABLED=false` remains the documented/default safe state.
- Controlled writes remain experimental post-MVP and disabled by default.
- Phase 46/47 compatibility work does not expand write scope.
- `apps/api/tests/generated-fixtures/` is ignored in `.gitignore`.
- `git ls-files` did not show tracked files under `generated-fixtures/`.

## Fixture safety and personal-data review

No personal data or real financial data was found in the Phase 46 fixture generator/test/docs.

Reviewed:

- `apps/api/scripts/create_compatibility_fixture_v1.py`
- `apps/api/tests/test_compatibility_fixture_v1.py`
- `docs/gnucash-compatibility-fixture-v1.md`
- `docs/gnucash-compatibility.md`
- `.gitignore`

Findings:

- Account names are generic: Assets, Checking, Savings, Cash, Credit Card, Salary, Interest, Groceries, Utilities, Travel, Opening Balances.
- Transaction descriptions use `Fixture ...` fake labels only.
- Amounts are small synthetic decimal values and not tied to any real account/person/vendor.
- Metadata records non-sensitive provenance, schema versions, expected counts, and SHA-256 only.
- No screenshots, exports, `.env`, app DB, backups, keys, tokens, or real book paths were added.

## Large/unwanted binary review

No unwanted Phase 46 generated binary is tracked.

- `search_files` found only the two pre-existing committed synthetic SQLite test fixtures:
  - `apps/api/tests/fixtures/test-book.gnucash.sqlite`
  - `apps/api/tests/fixtures/test-book-multicurrency.gnucash.sqlite`
- `git ls-files` large-file scan found no tracked files larger than 1 MiB.
- `git ls-files` suspicious-path scan found no tracked generated fixture, `.env`, app DB, backup, secret, token, or key artifact outside expected docs/examples.

## Release/readme/docs consistency

Mostly consistent; one release-facing status mismatch was accepted for Phase 47 engineer cleanup:

- `README.md` still pointed at the previous release-gate audit as the latest audit and said Phase 0–46 were complete before Phase 47 status sync.
- `PROJECT_STATUS.md` still listed Phase 47 as the next planned phase before Phase 47 status sync.
- `CHANGELOG.md` did not yet include the Phase 47 audit entry before Phase 47 status sync.

These are normal end-of-phase status updates, not blockers.

## GitHub project hygiene

- Related issue: GitHub #22, `Add compatibility fixtures from real GnuCash versions`, is open and correctly remains relevant because Phase 46 is generated-fixture coverage rather than real Desktop-version fixture coverage.
- Phase 47 should update #22 with the audit result and leave it open unless the maintainer decides generated fixture coverage is sufficient.

## Security notes

No new security issue found in this phase.

- No auth storage changes were made.
- No secrets or credentials were found in the reviewed Phase 46 fixture artifacts.
- The fixture generator writes local files only and does not introduce network access or telemetry.

## Test/CI notes

Required/relevant checks for this phase:

- `cd apps/api && pytest -q tests/test_compatibility_fixture_v1.py`
- `cd apps/api && pytest -q`
- `cd apps/web && npm run check && npm run test:auth-routes && npm run build`
- `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet`
- `git diff --check`

The handoff file records the actual pass/fail results.

## Recommended next actions

1. Complete Phase 47 status sync and handoff.
2. Update GitHub issue #22 with the audit result.
3. Proceed to Phase 48 read-only UX polish only after Phase 47 commit is pushed and the working tree is clean.
4. Keep desktop-version compatibility fixtures as future work unless issue #22 is explicitly re-scoped.

## Suggested GitHub issues

No new issue required.

Suggested update to existing issue #22:

- Note that Phase 47 audit found the generated fixture path safe and conservative.
- Keep the issue open for real GnuCash Desktop version fixtures unless maintainers intentionally narrow the issue.

## What not to do next

- Do not claim broad GnuCash Desktop compatibility from the generated `piecash` fixture alone.
- Do not commit generated `.gnucash.sqlite` files from `apps/api/tests/generated-fixtures/`.
- Do not enable controlled writes by default.
- Do not expand write scope while closing compatibility documentation gaps.
- Do not publish a new release/tag from Phase 47.
