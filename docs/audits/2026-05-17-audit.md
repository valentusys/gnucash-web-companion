# Independent Audit — 2026-05-17

## Executive summary
Repository remains aligned with the read-only-first pre-alpha product boundary, and no default-write or financial-data leak regression was found. The release/safety blocker for Phase 29 is documentation drift: README, CHANGELOG, roadmap, and `v0.0.2-prealpha` candidate notes lag behind completed Phases 26–28, while `PROJECT_STATUS.md` is current only through Phase 28. Fix release-state documentation before adding another feature.

## Verdict
Ready only after blockers are fixed

## Top blockers
1. Public release/status docs are out of sync: README still says Phase 0–25 are complete, CHANGELOG and `docs/release/v0.0.2-prealpha-notes.md` stop at Phase 24, and `docs/ROADMAP.md` still frames `v0.0.1` pre-alpha as the next step even though `v0.0.1-prealpha` exists.
2. `docs/handoff/phase-28.md` still has a pending commit placeholder even though Phase 28 was pushed as `343e098`.

## Important non-blockers
1. GitHub open issues exist (#11–#17), so project hygiene is not empty; remaining issues are backlog/community/read-only improvements rather than immediate safety blockers.
2. GitHub release state is coherent: `v0.0.1-prealpha` exists as a pre-release; `v0.0.2-prealpha` is not published and must not be published without explicit Val approval.
3. The local worktree has untracked `.hermes/`; it must remain uncommitted.
4. `docs/ROADMAP.md` is concise but stale after many phases; it should be updated as a release-readiness document, not expanded into feature creep.

## Product consistency
README, AGENTS, PROJECT_STATUS, SECURITY, and safety docs consistently position the project as pre-alpha, self-hosted, not SaaS, not a GnuCash replacement, not collaborative accounting, and read-only by default. Controlled writes remain described as experimental/post-MVP.

## Safety boundary
No default write enablement was found. `.env.example` and `docker-compose.yml` keep `GNUCASH_WRITES_ENABLED=false` as the default. Synthetic `.gnucash.sqlite` fixtures are intentional test fixtures. No tracked `.env`, app DB, backup, key, cert, token, real screenshot, or real financial export was found in this pass.

## Release/readme/docs consistency
`PROJECT_STATUS.md` is current through Phase 28, but several public-facing docs lag behind:

- README current status: Phase 0–25 only.
- CHANGELOG: Unreleased entries stop at Phase 24.
- `docs/release/v0.0.2-prealpha-notes.md`: candidate summary stops at Phase 24 and still says compatibility matrix is TBD.
- `docs/ROADMAP.md`: still says next is `v0.0.1` pre-alpha.
- `docs/handoff/phase-28.md`: commit field still says pending.

These are release-readiness blockers because they can mislead reviewers about what is actually in `main`.

## GitHub project hygiene
`gh` is authenticated as `valentusys`. Open issues observed:

- #17 Plan Russian documentation and UI localization
- #14 CSV export amount range filters in frontend
- #13 Book management UI
- #12 Scheduled/recurring transaction awareness
- #11 Transaction search/filter improvements

No new GitHub issue was created for the documentation drift because Phase 29 should fix it immediately.

## Security notes
JWT secret placeholder remains documented as unsafe and required to be changed. Auth token storage remains httpOnly-cookie based; no auth localStorage/sessionStorage usage was found beyond docs/tests warning about the rule. SECURITY.md honestly says pre-alpha, not audited, and not production-ready.

## Test/CI notes
Full tests were not run during the audit-only inspection. Latest Phase 28 handoff reports backend 264 passed, frontend check/auth-routes/build OK, and Docker config OK. Phase 29 must rerun the standard backend, frontend, and Docker config checks after documentation changes.

## Recommended next actions
1. Make Phase 29 an audit-driven documentation/release-state synchronization phase.
2. Update README, CHANGELOG, roadmap, `v0.0.2-prealpha` notes, `PROJECT_STATUS.md`, and Phase 28/29 handoff docs.
3. Keep `GNUCASH_WRITES_ENABLED=false`; do not expand write capability.
4. Do not publish tags or releases.
5. After status docs are fixed, continue with read-only MVP release-readiness work such as clean-machine Docker/E2E validation, frontend amount-filter UX for CSV export, or privacy/read-only indicators.

## Suggested GitHub issues
No new issue created in this audit pass. Existing relevant issues remain:

1. #14 CSV export amount range filters in frontend — labels: enhancement, read-only.
2. #11 Transaction search/filter improvements — labels: enhancement, read-only.
3. #17 Plan Russian documentation and UI localization — labels: documentation, community, post-MVP, good-first-issue.

## What not to do next
- Do not publish `v0.0.2-prealpha` tags/releases without explicit Val approval.
- Do not add new write-mode surface while release/status docs are stale.
- Do not claim production readiness or security audit completion.
- Do not broaden the MVP into collaborative accounting, hosted SaaS, banking integrations, CSV/OFX import, or family-wallet positioning.

---

## Background mission phase 1 — 2026-05-17

### Scope
Independent auditor-only pass for guarded background mission cycle 1. No product fixes were implemented. This pass focused on backend write-gating by real API behavior, Phase 28+ documentation consistency, controlled-write stale-claim cleanup, and required GitHub issue hygiene.

### Verdict
Ready only after blockers are fixed.

This is not an urgent write-gating failure verdict: the real API write-gating check passed. The release still needs follow-up because required audit-tracking issues were absent before this pass and the write-gating bypass protection should be made durable as a committed regression test.

### Top blockers
1. The backend write feature flag is currently effective by API behavior, but the repository has only a committed validate-endpoint disabled-writes test. It does not yet have a committed regression test proving `GnuCashWriteService` is not instantiated when writes are disabled for validate/create/patch. Created issue #18 for PM/engineer follow-up.
2. `docs/v0.2-controlled-writes.md` still keeps the old limitation text for in-process lock, missing real integration tests, and backup restore smoke coverage as struck-through historical notes. These are not active stale claims, but PM/engineer should either keep them clearly resolved or remove them in the next controlled-writes doc cleanup.

### Write-gating audit result
Real API test passed.

Command run from `apps/api`:

```bash
python - <<'PY'
# ad-hoc FastAPI TestClient audit script:
# - Settings.gnucash_writes_enabled=False
# - in-memory app DB with admin owner and synthetic book metadata
# - monkeypatch app.routers.transactions._write_service_for to raise if called
# - POST /books/{book_id}/transactions/validate
# - POST /books/{book_id}/transactions
# - PATCH /books/{book_id}/transactions/fake-tx-guid
# Expected: all three return 403 with read-only detail and zero write-service calls.
PY
```

Observed output:

```text
PASS real API write-gating test: validate/create/patch returned 403 and GnuCashWriteService was not instantiated
```

Static cross-check:

- `apps/api/app/config.py`: `Settings.gnucash_writes_enabled: bool = False` is present.
- `apps/api/app/routers/transactions.py`: `_ensure_writes_enabled(settings)` exists and raises 403 before book resolution/write-service creation.
- `validate_book_transaction`, `create_book_transaction`, and `patch_book_transaction` all call `_ensure_writes_enabled(settings)` before `_write_service_for(book)`.

Targeted committed test also passed:

```bash
cd apps/api && pytest -q tests/test_transaction_writes.py::TestWritesDisabledByDefault::test_validate_is_forbidden_when_writes_disabled
```

Result: `1 passed, 1 warning`.

### Documentation consistency checks
- README: honest current baseline; says Phase 0–31 complete, which satisfies the requirement for Phase 0–28 or later honest baseline.
- PROJECT_STATUS.md: updated through Phase 31 and includes Phase 28.
- CHANGELOG.md: Unreleased includes Phase 17 through Phase 31, including all Phase 17–28 entries.
- Latest handoff: `docs/handoff/phase-31.md` exists and states Phase 31 complete.
- Controlled writes doc: no active claim that lock is in-process-only, integration tests are still required, or backup restore coverage is still needed. Those lines are struck through and marked resolved for Phases 21–23.

### GitHub project hygiene
`gh` authenticated as `valentusys`. Required open issues were missing before this pass and were created:

- #18 Verify backend write feature flag cannot be bypassed — https://github.com/valentusys/gnucash-web-companion/issues/18
- #19 Sync README/PROJECT_STATUS/CHANGELOG after Phase 28 — https://github.com/valentusys/gnucash-web-companion/issues/19
- #20 Prepare v0.0.2-prealpha release — https://github.com/valentusys/gnucash-web-companion/issues/20
- #21 Add UI warning before write mode — https://github.com/valentusys/gnucash-web-companion/issues/21
- #22 Add compatibility fixtures from real GnuCash versions — https://github.com/valentusys/gnucash-web-companion/issues/22

Labels `audit` and `release` were created because they were absent.

### PM/engineer instructions
Do not restore write gating code because it is not broken in this audit. Instead, add durable regression coverage for the API-gating behavior proven by the ad-hoc audit:

- assert disabled validate/create/patch endpoints return 403;
- assert `_write_service_for` / `GnuCashWriteService` is not called when disabled;
- keep `Settings.gnucash_writes_enabled: bool = False`;
- keep `_ensure_writes_enabled()` before any book write-service construction;
- keep write mode post-MVP and disabled by default.

If a later phase breaks gating, treat it as an URGENT BLOCKER and restore exactly:

- `Settings.gnucash_writes_enabled: bool = False`
- dependency/helper `_ensure_writes_enabled()`
- checks in validate/create/patch endpoints
- a test that `GnuCashWriteService` is not instantiated when writes are disabled

### Recommended next actions
1. PM: brief engineer to turn the ad-hoc real API write-gating audit into a committed regression test for validate/create/patch.
2. Engineer: keep changes test-only unless a regression is found; do not expand write capability.
3. PM/engineer: decide whether to remove struck-through resolved limitation text from `docs/v0.2-controlled-writes.md` or leave it as explicit historical resolution.
4. Continue release-readiness work toward `v0.0.2-prealpha`; do not publish tags/releases without explicit approval.

### What not to do next
- Do not enable writes by default.
- Do not add new write-mode features before the bypass regression test exists.
- Do not claim production safety, broad GnuCash version compatibility, or audited write safety.

---

## Background mission phase 4 — 2026-05-17

### Scope
Independent auditor-only pass for guarded background mission cycle 2. No product fixes were implemented. This pass re-checked backend write-gating with a real FastAPI API test, re-checked Phase 28+ public status docs, reviewed `docs/v0.2-controlled-writes.md`, and verified the required GitHub issues are open.

### Verdict
Ready only after documentation blockers are fixed.

No urgent backend write-gating blocker was found: the API-level disabled-write test passed, and the committed route ordering still blocks validate/create/patch before any write service construction. The main blockers are documentation hygiene: README is one phase behind current status, and the controlled-writes design doc still contains stale active wording about the old in-process lock.

### Top blockers
1. `docs/v0.2-controlled-writes.md` still has an active stale bullet under “Existing experimental implementation”: `in-process write lock`. Phase 21 replaced this with file-based `fcntl.flock()` locking, so PM/engineer should clean the doc. Created GitHub issue #23.
2. README current status says Phase 0–31 are complete while `PROJECT_STATUS.md` and latest handoff show Phase 32 is complete. This satisfies the minimum Phase 0–28 visibility requirement, but it is not a fully honest current baseline after Phase 32. Keep issue #19 open until release/status docs are synchronized.

### Write-gating audit result
Real API test passed.

Command run from `apps/api`:

```bash
pytest -q tests/test_transaction_writes.py::TestWritesDisabledByDefault
```

Observed result:

```text
4 passed, 1 warning
```

Coverage verified in the committed API tests:

- `Settings().gnucash_writes_enabled is False` remains asserted.
- `POST /books/{book_id}/transactions/validate` returns 403 with writes disabled.
- `POST /books/{book_id}/transactions` returns 403 with writes disabled.
- `PATCH /books/{book_id}/transactions/{transaction_id}` returns 403 with writes disabled.
- The tests monkeypatch `app.routers.transactions._write_service_for` to raise and assert it is not called, proving `GnuCashWriteService` is not constructed for disabled-write requests.

Static cross-check:

- `apps/api/app/config.py`: `Settings.gnucash_writes_enabled: bool = False` remains present.
- `apps/api/app/routers/transactions.py`: `_ensure_writes_enabled(settings)` exists and raises HTTP 403 with read-only wording.
- `validate_book_transaction`, `create_book_transaction`, and `patch_book_transaction` call `_ensure_writes_enabled(settings)` before `_resolve_viewable_book`, `_require_book_edit_access`, and `_write_service_for(book)`.

### Documentation consistency checks
- README: says Phase 0–31 complete. This covers Phase 0–28, but is one phase behind the current Phase 32 baseline.
- `PROJECT_STATUS.md`: updated through Phase 32.
- `CHANGELOG.md`: Unreleased includes Phase 17 through Phase 32, including all Phase 17–28 entries.
- Latest handoff: `docs/handoff/phase-32.md` exists and states Phase 32 complete, with verification and pushed commits.
- `docs/v0.2-controlled-writes.md`: stale active wording remains for `in-process write lock`; historical struck-through resolved limitations for integration tests and backup restore are understandable but should be removed or rewritten if the project wants a clean current-state design doc.

### GitHub project hygiene
`gh` authenticated as `valentusys`. Required open issues are now present:

- #18 Verify backend write feature flag cannot be bypassed — reopened during this audit because the mission explicitly requires it open.
- #19 Sync README/PROJECT_STATUS/CHANGELOG after Phase 28.
- #20 Prepare v0.0.2-prealpha release.
- #21 Add UI warning before write mode.
- #22 Add compatibility fixtures from real GnuCash versions.

New issue created:

- #23 Clean stale controlled-writes documentation after lock/test/restore safety phases — tracks the stale `docs/v0.2-controlled-writes.md` wording found in this audit.

### PM/engineer instructions
Do not restore write-gating code because it is not broken in this audit. Keep the following intact:

- `Settings.gnucash_writes_enabled: bool = False`
- `_ensure_writes_enabled()`
- checks in validate/create/patch endpoints before write-service construction
- the committed tests proving `GnuCashWriteService` / `_write_service_for` is not instantiated when writes are disabled

If a later phase breaks any of those, treat it as an URGENT BLOCKER and restore the exact items above before continuing release work.

### Recommended next actions
1. PM: brief engineer to clean stale controlled-writes documentation (#23) and sync README status with Phase 32 (#19), without expanding write scope.
2. Engineer: keep changes documentation-only unless a new regression is found.
3. Keep #18 open until PM/release triage confirms the current mission can close it despite the explicit open-issue requirement.
4. Do not publish `v0.0.2-prealpha` without explicit Val approval.

### What not to do next
- Do not enable writes by default.
- Do not treat the passing write-gating tests as a production-safety claim.
- Do not add new write-mode features before UI warning, compatibility fixtures, and release docs are handled.

---

## Background mission phase 7 — 2026-05-17

### Scope
Independent auditor-only pass for guarded background mission cycle 3. No product fixes were implemented. This pass re-checked backend write-gating with a real FastAPI API test, reviewed Phase 28+ public status docs after Phase 33, checked stale controlled-writes claims, and verified required GitHub issue hygiene.

### Verdict
Ready only after documentation baseline is synchronized.

No urgent backend write-gating blocker was found. The API-level disabled-write regression test passed, `Settings.gnucash_writes_enabled` remains `False` by default, `_ensure_writes_enabled()` is present, and validate/create/patch routes call it before `_write_service_for()` / `GnuCashWriteService` construction.

### Top blockers
1. README current status says Phase 0–32 are complete while `PROJECT_STATUS.md` and the latest handoff show Phase 33 is complete. This still satisfies the minimum Phase 0–28 requirement, but it is no longer the fully current public baseline. Issue #19 was reopened for the next PM/engineer sync.
2. `v0.0.2-prealpha` is still not published. That is correct without explicit Val approval, but release preparation remains open as issue #20.

### Write-gating audit result
Real API test passed.

Command run from `apps/api`:

```bash
pytest -q tests/test_transaction_writes.py::TestWritesDisabledByDefault
```

Observed result:

```text
4 passed, 1 warning
```

Coverage verified in the committed API tests:

- `Settings().gnucash_writes_enabled is False` remains asserted.
- `POST /books/{book_id}/transactions/validate` returns 403 with writes disabled.
- `POST /books/{book_id}/transactions` returns 403 with writes disabled.
- `PATCH /books/{book_id}/transactions/{transaction_id}` returns 403 with writes disabled.
- The tests monkeypatch `app.routers.transactions._write_service_for` to raise and assert it is not called, proving `GnuCashWriteService` is not constructed for disabled-write requests.

Static cross-check:

- `apps/api/app/config.py`: `Settings.gnucash_writes_enabled: bool = False` remains present.
- `apps/api/app/routers/transactions.py`: `_ensure_writes_enabled(settings)` exists and raises HTTP 403 with read-only wording.
- `validate_book_transaction`, `create_book_transaction`, and `patch_book_transaction` call `_ensure_writes_enabled(settings)` before `_resolve_viewable_book`, `_require_book_edit_access`, and `_write_service_for(book)`.

### Documentation consistency checks
- README: says Phase 0–32 complete. This covers Phase 0–28, but is one phase behind the current Phase 33 baseline.
- `PROJECT_STATUS.md`: updated through Phase 33.
- `CHANGELOG.md`: Unreleased includes Phase 17 through Phase 33, including all Phase 17–28 entries.
- Latest handoff: `docs/handoff/phase-33.md` exists and states Phase 33 complete, with verification and pushed status.
- `docs/v0.2-controlled-writes.md`: no active stale claim remains for “in-process lock only”, “integration tests still required”, or “backup restore needed”. Current wording documents file-based `fcntl.flock()` locking, Phase 22 real disposable-book integration tests, Phase 23 backup restore smoke coverage, and Phase 32 disabled-write bypass regression coverage.

### GitHub project hygiene
`gh` authenticated as `valentusys`. Required issues are present and open after this audit:

- #18 Verify backend write feature flag cannot be bypassed.
- #19 Sync README/PROJECT_STATUS/CHANGELOG after Phase 28 — reopened during this audit because README is one phase behind and the guarded mission explicitly requires it open.
- #20 Prepare v0.0.2-prealpha release.
- #21 Add UI warning before write mode.
- #22 Add compatibility fixtures from real GnuCash versions.

No new issue was needed. Issue #19 was reopened with an audit comment.

### PM/engineer instructions
Do not restore write-gating code because it is not broken in this audit. Keep the following intact:

- `Settings.gnucash_writes_enabled: bool = False`
- `_ensure_writes_enabled()`
- checks in validate/create/patch endpoints before write-service construction
- the committed tests proving `GnuCashWriteService` / `_write_service_for` is not instantiated when writes are disabled

If a later phase breaks any of those, treat it as an URGENT BLOCKER and restore the exact items above before continuing release work.

### Recommended next actions
1. PM: brief engineer to sync README current status through Phase 33 or another honest current baseline, then decide whether #19 can close again.
2. PM/engineer: keep #18, #20, #21, and #22 open until explicit release/backlog triage says otherwise.
3. Do not publish `v0.0.2-prealpha` without explicit Val approval.
4. Continue read-only/release-readiness work before expanding write-mode scope.

### What not to do next
- Do not enable writes by default.
- Do not treat the passing write-gating tests as a production-safety claim.
- Do not add new write-mode features before UI warning, compatibility fixtures, and release docs are handled.

---

## Background mission phase 10 — 2026-05-17

### Scope
Independent auditor-only pass for guarded background mission cycle 4. No product fixes were implemented. This pass re-checked backend write-gating with a real FastAPI API test, reviewed public status docs after Phase 34, checked controlled-writes stale claims, and verified the required GitHub issue hygiene.

### Verdict
Ready only after documentation baseline is synchronized.

No urgent backend write-gating blocker was found. The API-level disabled-write regression test passed, `Settings.gnucash_writes_enabled` remains `False` by default, `_ensure_writes_enabled()` is present, and validate/create/patch routes call it before `_write_service_for()` / `GnuCashWriteService` construction.

### Top blockers
1. README current status says Phase 0–33 are complete while `PROJECT_STATUS.md` and the latest handoff show Phase 34 is complete. This still satisfies the minimum Phase 0–28 visibility requirement, but it is no longer the fully current public baseline after Phase 34. Issue #19 was reopened again for PM/engineer triage because this guarded mission explicitly requires it open.
2. `docs/v0.2-controlled-writes.md` no longer contains the stale active claims requested for removal (`in-process lock only`, `integration tests still required`, `backup restore needed`), but it does contain a separate stale active known-limitation line: `CSV export (Phase 24) is read-only; amount range filters are backend-only in the frontend.` Phase 30 added frontend amount range filters, so PM/engineer should clean this as documentation hygiene.
3. `v0.0.2-prealpha` is still not published. That remains correct without explicit Val approval, but release preparation remains open as issue #20.

### Write-gating audit result
Real API test passed.

Command run from `apps/api`:

```bash
pytest -q tests/test_transaction_writes.py::TestWritesDisabledByDefault
```

Observed result:

```text
4 passed, 1 warning
```

Coverage verified in the committed API tests:

- `Settings().gnucash_writes_enabled is False` remains asserted.
- `POST /books/{book_id}/transactions/validate` returns 403 with writes disabled.
- `POST /books/{book_id}/transactions` returns 403 with writes disabled.
- `PATCH /books/{book_id}/transactions/{transaction_id}` returns 403 with writes disabled.
- The tests monkeypatch `app.routers.transactions._write_service_for` to raise and assert it is not called, proving `GnuCashWriteService` is not constructed for disabled-write requests.

Static cross-check:

- `apps/api/app/config.py`: `Settings.gnucash_writes_enabled: bool = False` remains present.
- `apps/api/app/routers/transactions.py`: `_ensure_writes_enabled(settings)` exists and raises HTTP 403 with read-only wording.
- `validate_book_transaction`, `create_book_transaction`, and `patch_book_transaction` call `_ensure_writes_enabled(settings)` before `_resolve_viewable_book`, `_require_book_edit_access`, and `_write_service_for(book)`.

### Documentation consistency checks
- README: says Phase 0–33 complete. This covers Phase 0–28, but is one phase behind the current Phase 34 baseline.
- `PROJECT_STATUS.md`: updated through Phase 34.
- `CHANGELOG.md`: Unreleased includes Phase 17 through Phase 34, including all Phase 17–28 entries.
- Latest handoff: `docs/handoff/phase-34.md` exists and states Phase 34 complete, with verification and pushed status.
- `docs/v0.2-controlled-writes.md`: no active stale claim remains for “in-process lock only”, “integration tests still required”, or “backup restore needed”. Current wording documents file-based `fcntl.flock()` locking, Phase 22 real disposable-book integration tests, Phase 23 backup restore smoke coverage, and Phase 32 disabled-write bypass regression coverage. Separate stale limitation found: amount range filters are no longer backend-only in the frontend after Phase 30.

### GitHub project hygiene
`gh` authenticated as `valentusys`. Required issues are present and open after this audit:

- #18 Verify backend write feature flag cannot be bypassed.
- #19 Sync README/PROJECT_STATUS/CHANGELOG after Phase 28 — reopened during this audit because README is one phase behind and the guarded mission explicitly requires it open.
- #20 Prepare v0.0.2-prealpha release.
- #21 Add UI warning before write mode.
- #22 Add compatibility fixtures from real GnuCash versions.

No new issue was needed. Issue #19 was reopened with an audit comment.

### PM/engineer instructions
Do not restore write-gating code because it is not broken in this audit. Keep the following intact:

- `Settings.gnucash_writes_enabled: bool = False`
- `_ensure_writes_enabled()`
- checks in validate/create/patch endpoints before write-service construction
- the committed tests proving `GnuCashWriteService` / `_write_service_for` is not instantiated when writes are disabled

If a later phase breaks any of those, treat it as an URGENT BLOCKER and restore the exact items above before continuing release work.

### Recommended next actions
1. PM: brief engineer to sync README current status through Phase 34 or another explicitly honest current baseline, then decide whether #19 can close again.
2. PM/engineer: clean the stale `docs/v0.2-controlled-writes.md` known-limitation line about amount range filters being backend-only in the frontend.
3. PM/engineer: keep #18, #20, #21, and #22 open until explicit release/backlog triage says otherwise.
4. Do not publish `v0.0.2-prealpha` without explicit Val approval.
5. Continue read-only/release-readiness work before expanding write-mode scope.

### What not to do next
- Do not enable writes by default.
- Do not treat the passing write-gating tests as a production-safety claim.
- Do not add new write-mode features before UI warning, compatibility fixtures, and release docs are handled.
