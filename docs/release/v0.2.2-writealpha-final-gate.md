# v0.2.2-writealpha Final Release-Readiness Gate

Date: 2026-05-20
Phase: 191
Target release candidate: `v0.2.2-writealpha`
Verdict: `PASS — publish as conservative GitHub pre-release after exact release-commit CI succeeds`

## Scope

This gate compares current `main`, README/README.ru/PROJECT_STATUS/CHANGELOG, release artifacts, dogfood evidence, GitHub releases/actions, open issues, and safety defaults after cycle-2 Phases 183–190.

The phase contract authorizes publication only if evidence warrants it and the exact release commit is green. Evidence warrants a write-alpha maintenance pre-release because Phases 183–190 materially changed or validated write-alpha safety/evidence paths after `v0.2.1-writealpha`: lock recovery, PATCH/DELETE disposable dogfood, audit summary visibility, multi-book read-only regression hardening, reporting correctness, fresh-clone smoke, and combined release-candidate dogfood.

## Release target decision

- Selected target: `v0.2.2-writealpha`.
- Rationale: cycle-2 changes include write-alpha evidence and operator/audit/readiness improvements beyond the published `v0.2.1-writealpha`; this is not read-only-only scope.
- Release type: GitHub pre-release only.
- Not published: packages, binary artifacts, Docker images, production deployment, public hosting, or any broader compatibility claim.

## Repository and GitHub state before release artifacts

| Item | Result |
| --- | --- |
| Branch | `main` |
| Starting HEAD | `bdaca3b2b3fbe05e00315f916b3a9374df6cc9b6` |
| Starting `origin/main` | `bdaca3b2b3fbe05e00315f916b3a9374df6cc9b6` |
| Working tree | clean tracked tree; only untracked `.hermes/` excluded |
| Current public read-only release | `v0.1.7-readonly` |
| Current public write-alpha release | `v0.2.1-writealpha` |
| Candidate local tag before publication | absent |
| Candidate remote tag before publication | absent |
| Candidate GitHub release before publication | absent |
| GitHub auth | available via `gh auth status` |
| Open issues reviewed | #36, #29, #28, #22, #17, #13 remain open; none block this conservative write-alpha maintenance pre-release because the release notes keep those limitations/non-claims explicit |

## Documentation/status comparison

| Area | Finding | Result |
| --- | --- | --- |
| `README.md` / `README.ru.md` | Public status still stopped at Phase 182 before this phase. | Updated to Phase 191 and `v0.2.2-writealpha` publication status. |
| `PROJECT_STATUS.md` | Current baseline stopped at Phase 190. | Updated to completed Phase 191 and published `v0.2.2-writealpha`. |
| `CHANGELOG.md` | `Unreleased` only listed Phase 183. | Updated with `0.2.2-writealpha` section for Phases 183–191. |
| Release docs | No `v0.2.2-writealpha` artifacts existed. | Added notes, checklist, final gate, and publication evidence. |
| Dogfood evidence | Phase 183–190 docs record synthetic/disposable-only evidence and default false resets. | Accepted with limits; no real/private-book write safety claim. |
| Safety wording | Must remain conservative. | Release notes/checklist/final gate deny production/security/real-private-book write safety claims. |

## Required local checks

| Area | Command / evidence | Result |
| --- | --- | --- |
| Working tree and parity | `git status --short --branch`; `git fetch origin main --tags --prune`; `git rev-parse HEAD origin/main` | PASS before release artifact edits. |
| Release absence | `git tag -l v0.2.2-writealpha`; `git ls-remote --tags origin refs/tags/v0.2.2-writealpha`; `gh release view v0.2.2-writealpha ...` | PASS — absent before publication. |
| `.env.example` default | `grep -n 'GNUCASH_WRITES_ENABLED' .env.example` | PASS — `GNUCASH_WRITES_ENABLED=false`. |
| Docker Compose config | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet` | PASS. |
| Rendered write default | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config \| grep -n 'GNUCASH_WRITES_ENABLED'` | PASS — API and web render `GNUCASH_WRITES_ENABLED: "false"`. |
| Backend suite | `cd apps/api && pytest -q` | PASS in Phase 191. |
| Frontend checks | `cd apps/web && npm run check && npm run test:auth-routes && npm run build` | PASS in Phase 191. |
| Whitespace diff check | `git diff --check` | PASS in Phase 191. |
| Sensitive tracked-file scan | Python scan over `git ls-files` with known synthetic/docs allowlist | PASS in Phase 191. |

## GitHub Actions gate

- Existing `main` CI for Phase 190 completed successfully before preparing the release commit.
- After the Phase 191 release/status commit was pushed, GitHub Actions for that exact commit were watched to successful completion before tag/release publication.
- Node.js 20 deprecation warnings from GitHub Actions were observed for `actions/checkout@v4`; they did not fail CI and are not a release blocker, but should be handled in a later maintenance phase.

## Write-alpha safety confirmation

| Check | Result |
| --- | --- |
| Default write posture | PASS — `GNUCASH_WRITES_ENABLED=false` remains default in `.env.example` and rendered Compose. |
| Test-environment write gate | PASS — write execution still requires explicit enablement plus `APP_ENV=test`. |
| Write scope | PASS — release does not add or broaden create/PATCH/DELETE routes. |
| Evidence scope | PASS — new write evidence is synthetic/disposable only; Phase 190 explicitly does not add a new restore claim where host-side root-owned readability limited helper final checks. |
| Product model | PASS — GnuCash Desktop remains authoritative; web app remains read-only by default. |
| Non-claims | PASS — release docs deny production readiness, security audit, broad compatibility, public-internet safety, and real/private/only-copy write safety. |

## Publication decision

Decision: publish `v0.2.2-writealpha` as a conservative GitHub pre-release after exact release-commit CI passes.

Rationale:

- A maintenance write-alpha pre-release is warranted by cycle-2 evidence and safety/operator hardening after published `v0.2.1-writealpha`.
- Local release-critical checks passed.
- Tag/release were absent before publication.
- Safety defaults and write gates remain intact.
- The phase contract explicitly authorizes publication if the gate is green.

## Remaining risks and non-claims

- Write-alpha remains experimental, disabled by default, and executable only with `APP_ENV=test` when explicitly enabled.
- Real/private-book write safety is not established and not claimed.
- Compatibility evidence remains intentionally narrow; no broad GnuCash Desktop/backend compatibility claim is made.
- CSV export remains synchronous and capped at 10,000 rows.
- The project remains pre-alpha, not production-ready, and not security-audited.
- Open issues #36, #29, #28, #22, #17, and #13 remain future-work/non-blocking under the conservative release wording.
