# v0.1.6-readonly Final Release Gate

Date: 2026-05-20
Phase: 161
Target release candidate: `v0.1.6-readonly`
Verdict: `Published as authorized GitHub pre-release`

## Scope

This release gate verifies the `v0.1.6-readonly` read-only maintenance candidate after Phases 153–160 and records the Phase 161 publication decision.

Phase 161 re-ran the final release gate and published only because the gate was green and Val authorized the release phase to publish if all gates passed. It created the annotated git tag and GitHub pre-release after checks passed. It did not create a package, binary artifact, Docker image, production deployment, write-mode default change, write-alpha expansion, or production-readiness claim.

## Checked revision

| Item | Result |
| --- | --- |
| Branch checked | `main` |
| Release phase starting HEAD | `d56c33546647917492b0909a3e23da760642a3c5` |
| Release phase starting `origin/main` | `d56c33546647917492b0909a3e23da760642a3c5` |
| Current public read-only release before Phase 161 | `v0.1.5-readonly` |
| Candidate prepared/published | `v0.1.6-readonly` |
| Gate artifact commit note | Phase 161 changes release/status/handoff documentation only before publication; no backend, frontend, Docker, auth, or write-mode code was changed. |

## Required checks

| Area | Command / evidence | Result |
| --- | --- | --- |
| Starting state | `git status --short --branch` | PASS — `## main...origin/main` plus pre-existing untracked `.hermes/` local agent data, intentionally not committed |
| HEAD/origin parity before artifacts | `git rev-parse HEAD` and `git rev-parse origin/main` | PASS — both `d56c33546647917492b0909a3e23da760642a3c5` |
| Prior HEAD CI | `gh run watch 26105165505 --exit-status` | PASS — CI for `d56c33546647917492b0909a3e23da760642a3c5` completed with `success` before release artifact commit |
| Backend full tests | `cd apps/api && pytest -q` | PASS — `386 passed, 32 warnings` |
| Frontend static check | `cd apps/web && npm run check` | PASS — `svelte-check found 0 errors and 0 warnings` |
| Frontend auth-route checks | `cd apps/web && npm run test:auth-routes` | PASS — `auth route checks passed` |
| Frontend build | `cd apps/web && npm run build` | PASS — production build completed |
| Docker Compose config | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet` | PASS — command exited successfully |
| Write-disabled Compose default | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config | grep -n 'GNUCASH_WRITES_ENABLED'` | PASS — API and web both render `GNUCASH_WRITES_ENABLED: "false"` |
| Whitespace diff check | `git diff --check` | PASS — no output |
| Tag absence | `git tag --list 'v0.1.6-readonly'` | PASS — no local tag output before publication |
| GitHub release absence | `gh release view v0.1.6-readonly --json tagName,url,isPrerelease,isDraft 2>&1 || true` | PASS — `release not found` before publication |
| GitHub Actions state | `gh run list --branch main --limit 5 --json status,conclusion,headSha,workflowName,createdAt,url` and post-push `gh run watch` | PASS — starting HEAD CI passed, and the Phase 161 release commit CI was watched to success before publication |
| Sensitive tracked-file scan | Python scan over `git ls-files` with known synthetic/runtime-doc allowlist | PASS — `sensitive tracked-file hygiene scan passed` |

## Read-only and write-disabled safety confirmation

| Check | Result |
| --- | --- |
| Default write posture | PASS — release docs retain `GNUCASH_WRITES_ENABLED=false`; Phase 161 made no settings or code changes. |
| Disabled-write coverage | PASS — backend full tests and Phase 160 dogfood include disabled write probes. |
| Controlled-write positioning | PASS — controlled writes remain post-MVP/experimental and are not marketed as safe for production books. |
| Product model | PASS — MVP model remains one installation, one local admin user, one default GnuCash book, read-only access. |
| Authoritative editor | PASS — release notes/checklist continue to state that GnuCash Desktop remains the authoritative editor. |

## Sensitive-data hygiene

Safe hygiene evidence only; no secret values or private user directories were inspected.

| Check | Result |
| --- | --- |
| Working tree before implementation | PASS — clean tracked tree on `main...origin/main`; untracked `.hermes/` local agent data was present and intentionally excluded from commit. |
| Changed-file scope | PASS — Phase 161 changes are release/status/handoff documentation only. |
| Prohibited artifacts | PASS — final tracked-file hygiene scan found no unexpected tracked private-data or secret artifacts. |
| Auth token handling | PASS — `gh release view` / `gh run list` / `gh run watch` were used only for release/CI state; no token values were printed into docs. |

## Release state

`v0.1.6-readonly` was absent before Phase 161 publication:

```text
git tag --list 'v0.1.6-readonly'
# no output

gh release view v0.1.6-readonly
# release not found
```

After Phase 161 checks passed, the annotated tag and GitHub pre-release were created:

```text
git tag -a v0.1.6-readonly -m "v0.1.6-readonly"
git push origin v0.1.6-readonly
gh release create v0.1.6-readonly --title "v0.1.6-readonly" --notes-file docs/release/v0.1.6-readonly-notes.md --prerelease
```

Release URL: https://github.com/valentusys/gnucash-web-companion/releases/tag/v0.1.6-readonly

No package, uploaded binary artifact, Docker image, or production deployment was created.

## Verdict

`Published as authorized GitHub pre-release`.

Phase 161 satisfied the authorized release contract: clean `main`, `HEAD == origin/main`, tag/release absence, GitHub Actions success for the release HEAD, local checks, `GNUCASH_WRITES_ENABLED=false`, and sensitive tracked-file hygiene were re-confirmed before creating the tag/release.

## Remaining risks and non-claims

- Publication happened in Phase 161; `v0.1.6-readonly` is now the current public read-only pre-alpha release.
- Compatibility evidence remains intentionally narrow; no broad GnuCash version/backend compatibility claim is made.
- Phase 160 dogfood evidence is synthetic/disposable only and not broad real-book or production evidence.
- CSV export remains synchronous and capped at 10,000 rows.
- Controlled writes remain post-MVP/experimental and disabled by default.
- The project remains pre-alpha, not production-ready, and not security-audited.
