# v0.2.1-writealpha Publication Evidence

Date: 2026-05-20
Phase: 182
Release: `v0.2.1-writealpha`
Status: published as an authorized GitHub pre-release after fresh gate
Release URL: https://github.com/valentusys/gnucash-web-companion/releases/tag/v0.2.1-writealpha

## Authorization

The Phase 182 contract explicitly authorized publication of the already prepared `v0.2.1-writealpha` candidate if a fresh pre-publish gate on current `main` passed and confirmed tag/GitHub release absence.

## Fresh pre-publish gate

Gate start revision before release/status docs were updated:

- `HEAD`: `ee134ea221db89a83388ba64967a8c3a41fd4c2b`
- `origin/main`: `ee134ea221db89a83388ba64967a8c3a41fd4c2b`
- tracked working tree: clean except untracked `.hermes/`, excluded from commit

Release-state checks before publication:

| Check | Result |
| --- | --- |
| Local tag `v0.2.1-writealpha` | absent |
| Remote tag `refs/tags/v0.2.1-writealpha` | absent |
| GitHub release `v0.2.1-writealpha` | absent (`release not found`) |
| GitHub auth | authenticated as `valentusys` |
| Recent GitHub Actions on `main` | Phase 181 and cycle-2 analyst runs completed successfully; release-doc commit CI was required before tag publication |

Safety/config checks before publication:

| Check | Result |
| --- | --- |
| `.env.example` | `GNUCASH_WRITES_ENABLED=false` |
| Rendered Docker Compose | API and web both render `GNUCASH_WRITES_ENABLED: "false"` |
| Docker Compose validation | passed with dummy local-only validation secrets |
| Backend write gates | existing tests keep writes disabled by default and require explicit enablement plus `APP_ENV=test` |
| Sensitive tracked-file hygiene | passed; no `.env`, app DB, real/private books, backups, exports, screenshots, tokens, keys, or certs tracked outside allowlisted fixtures/docs |

Local verification run during Phase 182:

```bash
git status --short
git fetch origin main --tags --prune
git rev-parse HEAD origin/main
git tag -l v0.2.1-writealpha
git ls-remote --tags origin refs/tags/v0.2.1-writealpha
gh auth status
gh release view v0.2.1-writealpha --json tagName,url,isPrerelease,isDraft,targetCommitish || true
gh run list --limit 10
JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet
JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config | grep -n 'GNUCASH_WRITES_ENABLED'
cd apps/api && pytest -q
cd apps/web && npm run check
cd apps/web && npm run test:auth-routes
cd apps/web && npm run build
git diff --check
# sensitive tracked-file hygiene scan over git ls-files
```

Results:

- Backend suite passed: `404 passed` with existing warnings.
- Frontend `npm run check` passed: 0 errors / 0 warnings.
- Frontend auth-route checks passed.
- Frontend production build passed.
- Docker Compose config validation passed.
- Rendered `GNUCASH_WRITES_ENABLED` stayed `"false"` for API and web.
- `git diff --check` passed before the release/status commit.
- Sensitive tracked-file hygiene scan passed before the release/status commit.

## Release commit and CI

Release/status commit and annotated tag target: `8c316b9f5c8028b519b603da0ba3cb37542bc4c0`.

Annotated tag object after publication: `a86bbcb3e70972b57eff89c1a305805f72ad0908`.

GitHub Actions requirement: CI on the exact release/status commit must complete successfully before tag/release publication.

Observed CI result before tag publication: GitHub Actions run `26140519337` for the exact Phase 182 release/status commit completed successfully before the tag and GitHub pre-release were created.

## Publication commands

Executed only after the fresh gate was green and CI passed for the exact release/status commit:

```bash
git tag -a v0.2.1-writealpha -m "v0.2.1-writealpha"
git push origin v0.2.1-writealpha
gh release create v0.2.1-writealpha \
  --title "v0.2.1-writealpha" \
  --notes-file docs/release/v0.2.1-writealpha-notes.md \
  --prerelease
```

## Post-publication verification

Post-publication verification to complete after tag/release creation:

```bash
git tag -l v0.2.1-writealpha
git ls-remote --tags origin refs/tags/v0.2.1-writealpha
gh release view v0.2.1-writealpha --json tagName,url,isPrerelease,isDraft,targetCommitish
git fetch origin main
test "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)"
git status --short
JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config | grep -n 'GNUCASH_WRITES_ENABLED'
```

Post-publication result: local tag, remote annotated tag, peeled remote tag target, and GitHub pre-release checks passed; `gh release view` reported `isPrerelease=true`, `isDraft=false`, `targetCommitish=main`, and the expected release URL.

## Safety result

Publication created only an annotated git tag and GitHub pre-release. It did not publish packages, binary artifacts, Docker images, production deployments, real/private books, app DBs, backups, `.env`, screenshots, exports, tokens, keys, certs, or private financial data.

`GNUCASH_WRITES_ENABLED=false` remains the documented/configured default. Write-alpha remains experimental, disabled by default, and executable only with explicit local enablement plus `APP_ENV=test`; it is still not safe for real/private or only-copy books and is not production-ready or security-audited.
