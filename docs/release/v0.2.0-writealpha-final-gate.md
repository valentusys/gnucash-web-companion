# v0.2.0-writealpha Final Release Gate

Date: 2026-05-19
Phase: 132
Target release candidate: `v0.2.0-writealpha`
Verdict: `Prepared for later authorization — unpublished / no tag / no release`

## Scope

This release gate prepares artifacts and records verification for a possible future `v0.2.0-writealpha` pre-alpha write-alpha pre-release after Phases 126–131.

Phase 132 does not publish a git tag, create a GitHub release, upload packages, enable writes by default, weaken the `APP_ENV=test` write gate, expand write scope, or claim production readiness. Publication remains pending separate explicit Val authorization.

## Checked revision

| Item | Result |
| --- | --- |
| Branch checked | `main` |
| HEAD checked before Phase 132 artifacts | `5fbe1f34f2e4f20e100b8ca34a4de360b09e6a4f` |
| `origin/main` before Phase 132 artifacts | `5fbe1f34f2e4f20e100b8ca34a4de360b09e6a4f` |
| Current public release before this phase | `v0.1.3-readonly` |
| Candidate prepared | `v0.2.0-writealpha` |
| Gate artifact commit note | The checks below were run during Phase 132. The phase changed release/status/handoff documentation only; no backend, frontend, Docker, auth, or write-mode code was changed. |

## Required checks

| Area | Command / evidence | Result |
| --- | --- | --- |
| Clean main / expected working tree | `git status --short --branch` | PASS — `## main...origin/main` plus only untracked `.hermes/` run artifacts, intentionally not committed |
| HEAD/origin parity | `git rev-parse HEAD` and `git rev-parse origin/main` | PASS before Phase 132 artifacts — both `5fbe1f34f2e4f20e100b8ca34a4de360b09e6a4f` |
| Backend test suite | `cd apps/api && pytest -q` | PASS — `377 passed, 32 warnings in 146.76s` |
| Frontend static check | `cd apps/web && npm run check` | PASS — `svelte-check found 0 errors and 0 warnings` |
| Frontend auth-route checks | `cd apps/web && npm run test:auth-routes` | PASS — `auth route checks passed` |
| Frontend production build | `cd apps/web && npm run build` | PASS — SvelteKit/Vite build completed successfully |
| Docker Compose config | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet` | PASS — command exited successfully with no output |
| Whitespace diff check | `git diff --check` | PASS — no output before release docs were committed; re-run after docs before commit also required |
| GitHub auth | `gh auth status` | PASS — authenticated; token output was not copied into committed files |
| GitHub Actions recent `main` state | `gh run list --branch main --limit 5 --json databaseId,status,conclusion,headSha,workflowName,createdAt,url` | PASS at gate time — latest five listed `main` CI runs were completed/success, including current pre-Phase-132 HEAD `5fbe1f34f2e4f20e100b8ca34a4de360b09e6a4f` |
| Tag absence | `git tag --list 'v0.2.0-writealpha'` | PASS — no local tag output |
| GitHub release absence | `gh release view v0.2.0-writealpha --json tagName,url,isPrerelease,isDraft 2>&1 || true` | PASS — `release not found` |
| Sensitive tracked-file scan | Python scan over `git ls-files` with known synthetic/runtime-doc allowlist | PASS — no unexpected tracked `.env`, app DB, runtime DB, private book, backup artifact, secret/token/cert/key, CSV export, or private media artifact paths; allowlist covers `.env.example`, `data/app/.gitkeep`, existing synthetic fixture books/screenshots, and existing backup docs/tests only |

## Recent GitHub Actions evidence

Latest five `main` runs reported during the Phase 132 gate were completed/success:

- `CI` for `5fbe1f34f2e4f20e100b8ca34a4de360b09e6a4f` — https://github.com/valentusys/gnucash-web-companion/actions/runs/26077916693
- `CI` for `e883cb36d1739f85771f66f6446663e2d71d4e9e` — https://github.com/valentusys/gnucash-web-companion/actions/runs/26077305383
- `CI` for `1b96a6e4dcd44eba2da280f8fa4175e4cafbba63` — https://github.com/valentusys/gnucash-web-companion/actions/runs/26076726925
- `CI` for `1b99b03466482272dfe66833a06b5d0f9aaa3bac` — https://github.com/valentusys/gnucash-web-companion/actions/runs/26076368043
- `CI` for `afea75b1d2d03eabda927241fa7f7b40d7d2a2fb` — https://github.com/valentusys/gnucash-web-companion/actions/runs/26076021450

After the Phase 132 commit is pushed, CI for the new docs/status commit should be observed separately. Publication still remains blocked until explicit Val authorization and immediate pre-publish checks.

## Write-alpha safety confirmation

| Check | Result |
| --- | --- |
| Default write posture | PASS — release docs retain `GNUCASH_WRITES_ENABLED=false`; Phase 132 made no settings or code changes. |
| Test-environment write gate | PASS — release docs require `APP_ENV=test`; Phase 132 did not weaken the backend gate. |
| Disabled-write coverage | PASS — backend full test suite includes disabled-write and write-alpha route regression coverage; Phase 132 did not enable writes. |
| Fixture scope | PASS — write evidence described in release notes is synthetic/disposable fixture evidence only. |
| Controlled-write positioning | PASS — controlled writes remain post-MVP/experimental and are not marketed as safe for production books. |
| Product model | PASS — GnuCash Desktop remains the authoritative editor; the web app remains read-only by default. |

## Sensitive-data hygiene

Safe hygiene evidence only; no secret values or private user directories were inspected.

| Check | Result |
| --- | --- |
| Working tree before implementation | PASS — clean tracked tree on `main...origin/main`; untracked `.hermes/` run artifacts were present and intentionally excluded from commit. |
| Changed-file scope | PASS — Phase 132 changes are release-prep/status/handoff documentation only. |
| Prohibited artifacts | PASS — no real/private financial data, GnuCash books, app DBs, backups, `.env`, secrets, tokens, certs, keys, screenshots, CSV exports, package uploads, or private media were created or added for this phase. |
| Auth token handling | PASS — `gh auth status` was used only for auth state; token values were not printed into docs. |

## Release state

`v0.2.0-writealpha` remains unpublished after the Phase 132 gate:

```text
git tag --list 'v0.2.0-writealpha'
(no output)

gh release view v0.2.0-writealpha
release not found
```

No tag, GitHub release, package, uploaded artifact, or publication was created in Phase 132.

## Verdict

`Prepared for later authorization — unpublished / no tag / no release`.

All required local Phase 132 release-gate checks passed, and release artifacts were prepared. A later publish phase may create `v0.2.0-writealpha` only after separate explicit Val authorization and immediate re-check of clean tree, HEAD/origin parity, tag/release absence, GitHub Actions success, backend/frontend/docker checks, `git diff --check`, default-disabled/test-gated write posture, and sensitive tracked-file hygiene.

## Remaining risks and non-claims

- Publication is not authorized in Phase 132.
- Write-alpha remains experimental, disabled by default, and executable only with `APP_ENV=test` when explicitly enabled.
- Real/private-book write safety is not established and not claimed.
- Compatibility evidence remains intentionally narrow; no broad GnuCash version/backend compatibility claim is made.
- CSV export remains synchronous and capped at 10,000 rows.
- The project remains pre-alpha, not production-ready, and not security-audited.
