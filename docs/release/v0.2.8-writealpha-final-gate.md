# v0.2.8-writealpha Final Gate Draft

Date: 2026-05-21
Phase: 260
Target release: `v0.2.8-writealpha`
Status: `RELEASE CANDIDATE ONLY — NOT PUBLISHED`
Verdict: `PASS — candidate prepared; Phase 261 must decide publish/no-release`

## Scope

This final-gate draft records the Phase 260 release-candidate/no-release decision for the Cycle 3 maintainer copied-book dogfood package after `v0.2.7-writealpha`.

Phase 260 prepares candidate artifacts only. It does not publish a tag, create a GitHub release, create a package/image, or claim that owner/private/original/only-copy books are safe for writes.

## Candidate rationale

| Area | Evidence | Result |
| --- | --- | --- |
| Maintainer dogfood packet | Phase 253 added a dry-run-first packet that forbids original/only-copy books, requires outside-git copied/restorable targets plus independent backups, limits optional mutation to one CREATE after review, defers PATCH, prohibits DELETE unless separately authorized, requires redacted evidence, restore proof, cleanup, and reset to `GNUCASH_WRITES_ENABLED=false`. | PASS |
| Command wrapper | Phase 254 added a local-only wrapper with separate `--dry-run` and `--create-one` modes, confirmations, redacted preflight, pre-step backup, evidence writing, unsafe-path rejection, and default-disabled reset proof. | PASS |
| UI warnings | Phase 255 strengthened EN/RU write-alpha warning copy for copied/restorable outside-git test books, original untouched, dry-run first, at most one CREATE test transaction, independent backup/restore, audit/app-backup/lock evidence, and no production use. | PASS |
| Compatibility harness | Phase 256 added a bounded best-effort harness that opens copied/disposable targets read-only with piecash, optionally probes already-available `gnucash-cli`, records missing tooling as blocked, and makes no broad Desktop/version compatibility claim. | PASS |
| Restore harness | Phase 257 added restore verification for outside-git copied working books from outside-git pre-mutation backups, with checksum/read-back proof and redacted evidence. | PASS |
| Synthetic package rehearsal | Phase 258 passed dry-run, create-one, read-back, restore verification, temporary Debian `gnucash-cli` report probing, default-disabled reset, and validate/create/PATCH/DELETE disabled probes on synthetic/disposable fixture copies. Browser evidence was not claimed. | PASS |
| Owner decision gate | Phase 259 concluded the next reasonable owner ask is copied-book dry-run only. CREATE-one is not the immediate ask and requires later owner dry-run evidence review. | PASS |

## Phase 260 local verification

| Check | Result |
| --- | --- |
| `python3 scripts/check_public_status.py` | PASS. |
| `cd apps/api && pytest -q` | PASS. |
| `cd apps/web && npm run check` | PASS. |
| `cd apps/web && npm run test:auth-routes` | PASS. |
| `cd apps/web && npm run build` | PASS. |
| `JWT_SECRET=<dummy-local-secret> APP_ADMIN_PASSWORD=<dummy-local-password> docker compose config --quiet` | PASS. |
| Rendered Compose write default | PASS — API and web keep `GNUCASH_WRITES_ENABLED=false`. |
| Safety greps for write flag, backend flag, `APP_ENV=test`, and browser storage | PASS — no weakening found. |
| `git diff --check` | PASS. |
| Sensitive tracked-file hygiene scan | PASS. |

## Safety confirmation

- `GNUCASH_WRITES_ENABLED=false` remains default.
- Explicit write-alpha execution remains constrained by `APP_ENV=test` and local enablement.
- No write endpoint, write scope, write default, or gate behavior changed in Phase 260.
- No create/PATCH/DELETE dogfood mutation was run in Phase 260.
- No owner/private/original/only-copy book, app DB, backup, `.env`, screenshot/export, token, key, cert, raw path, account name, memo, amount, or private financial data was committed.
- Owner copied-book dogfood remains pending or dry-run-only; this candidate does not authorize owner CREATE-one as the next step.
- Release notes deny production readiness, stable release status, security audit coverage, public-internet safety, broad compatibility, production disaster-recovery, and real/private or only-copy write safety.

## Publication boundary

Publication is explicitly out of scope for Phase 260.

If Phase 261 considers publication, it must call PM for release/no-release, rerun the full release gate, verify clean pushed release/status commit, wait for GitHub Actions on that exact commit, check local/remote tag absence and GitHub release absence, then publish only an annotated tag and GitHub pre-release if all gates pass.

No package, Docker image, binary artifact, production deployment, write-default change, `APP_ENV=test` gate weakening, or real/private-book safety claim is authorized.
