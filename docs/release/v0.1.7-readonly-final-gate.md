# v0.1.7-readonly Final Release Gate

Date: 2026-05-20
Phase: 171
Target release candidate: `v0.1.7-readonly`
Verdict: `Ready to publish after release commit CI passes`

## Scope

This release gate verifies the `v0.1.7-readonly` read-only maintenance candidate after Phases 162–170 and records the Phase 171 publication decision.

Phase 171 may publish only because the task scope explicitly authorizes tag/GitHub release publication when all gates are green. It must create only the annotated git tag and GitHub pre-release after checks pass. It must not create a package, binary artifact, Docker image, production deployment, write-mode default change, write-alpha expansion, or production-readiness claim.

## Checked revision

| Item | Result |
| --- | --- |
| Branch checked | `main` |
| Release phase starting HEAD | `cec8e5ed07d83335efcd5af72be89494ff932c0f` |
| Release phase starting `origin/main` | `cec8e5ed07d83335efcd5af72be89494ff932c0f` |
| Current public read-only release before Phase 171 | `v0.1.6-readonly` |
| Candidate prepared for publication | `v0.1.7-readonly` |
| Gate artifact commit note | Phase 171 changes release/status/handoff documentation only before publication; no backend, frontend, Docker, auth, or write-mode code was changed. |

## Required checks

| Area | Command / evidence | Result |
| --- | --- | --- |
| Starting state | `git status --short --branch` | PASS — `## main...origin/main` plus pre-existing untracked `.hermes/` local agent data, intentionally not committed |
| HEAD/origin parity before artifacts | `git rev-parse HEAD` and `git rev-parse origin/main` | PASS — both `cec8e5ed07d83335efcd5af72be89494ff932c0f` |
| Prior HEAD CI | `gh run list --branch main --limit 3 --json databaseId,status,conclusion,headSha,workflowName,url` | PASS — CI run `26115370807` for `cec8e5ed07d83335efcd5af72be89494ff932c0f` completed with `success` before Phase 171 documentation updates |
| Backend full tests | `cd apps/api && pytest -q` | PASS — `395 passed, 32 warnings` |
| Frontend static check | `cd apps/web && npm run check` | PASS — `svelte-check found 0 errors and 0 warnings` |
| Frontend auth-route checks | `cd apps/web && npm run test:auth-routes` | PASS — `auth route checks passed` |
| Frontend build | `cd apps/web && npm run build` | PASS — production build completed |
| Docker Compose config | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet` | PASS — command exited successfully |
| Write-disabled Compose default | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config | grep -n 'GNUCASH_WRITES_ENABLED'` | PASS — API and web both render `GNUCASH_WRITES_ENABLED: "false"` at lines 15 and 65 |
| Whitespace diff check | `git diff --check` | PASS — no output before artifact edits |
| Tag absence | `git tag --list 'v0.1.7-readonly'` | PASS — no local tag output before publication preparation |
| GitHub release absence | `gh release view v0.1.7-readonly --json tagName,url,isPrerelease,isDraft 2>&1 || true` | PASS — `release not found` before publication preparation |
| GitHub Actions state | `gh run list --branch main --limit 5 --json status,conclusion,headSha,workflowName,createdAt,url` and post-push `gh run watch` | STARTING HEAD PASS; release commit CI must pass before tag/release creation |
| Sensitive tracked-file scan | Python scan over `git ls-files` with known synthetic/runtime-doc allowlist | PASS — `sensitive tracked-file hygiene scan passed` |

## Read-only and write-disabled safety confirmation

| Check | Result |
| --- | --- |
| Default write posture | PASS — release docs retain `GNUCASH_WRITES_ENABLED=false`; Phase 171 made no settings or code changes. |
| Disabled-write coverage | PASS — backend full tests and Phase 170 dogfood include disabled write probes. |
| Controlled-write positioning | PASS — controlled writes remain post-MVP/experimental and are not marketed as safe for production books. |
| Product model | PASS — MVP model remains one installation, one local admin user, one default GnuCash book, read-only access. |
| Authoritative editor | PASS — release notes/checklist continue to state that GnuCash Desktop remains the authoritative editor. |

## Sensitive-data hygiene

Safe hygiene evidence only; no secret values or private user directories were inspected.

| Check | Result |
| --- | --- |
| Working tree before implementation | PASS — clean tracked tree on `main...origin/main`; untracked `.hermes/` local agent data was present and intentionally excluded from commit. |
| Changed-file scope | PASS — Phase 171 changes are release/status/handoff documentation only. |
| Prohibited artifacts | PASS — final tracked-file hygiene scan found no unexpected tracked private-data or secret artifacts. |
| Auth token handling | PASS — `gh release view` / `gh run list` / `gh run watch` are used only for release/CI state; no token values are written into docs. |

## Release state

`v0.1.7-readonly` was absent before Phase 171 publication preparation:

```text
git tag --list 'v0.1.7-readonly'
# no output

gh release view v0.1.7-readonly
# release not found
```

Publication commands are authorized for Phase 171 only after the release commit is pushed and CI for that exact commit passes:

```text
git tag -a v0.1.7-readonly -m "v0.1.7-readonly"
git push origin v0.1.7-readonly
gh release create v0.1.7-readonly --title "v0.1.7-readonly" --notes-file docs/release/v0.1.7-readonly-notes.md --prerelease
```

Release URL after publication: https://github.com/valentusys/gnucash-web-companion/releases/tag/v0.1.7-readonly

No package, uploaded binary artifact, Docker image, or production deployment is included.

## Verdict

`Ready to publish after release commit CI passes`.

Phase 171 has satisfied the local release contract so far: clean `main`, `HEAD == origin/main`, tag/release absence, GitHub Actions success for the starting HEAD, local checks, `GNUCASH_WRITES_ENABLED=false`, and sensitive tracked-file hygiene were confirmed before preparing the release artifacts. Tag/release publication remains gated on pushing this release commit and waiting for GitHub Actions on that exact commit to pass.

## Remaining risks and non-claims

- Publication is not complete until the Phase 171 release commit CI passes and the annotated tag/GitHub pre-release are created and verified.
- Compatibility evidence remains intentionally narrow; no broad GnuCash version/backend compatibility claim is made.
- Phase 170 dogfood evidence is synthetic/disposable only and not broad real-book or production evidence.
- CSV export remains synchronous and capped at 10,000 rows.
- Controlled writes remain post-MVP/experimental and disabled by default.
- The project remains pre-alpha, not production-ready, and not security-audited.
