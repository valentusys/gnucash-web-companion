# v0.1.5-readonly Final Release Gate

Date: 2026-05-19
Phase: 152
Target release candidate: `v0.1.5-readonly`
Verdict: `Published as authorized GitHub pre-release`

## Scope

This release gate verifies the prepared `v0.1.5-readonly` read-only maintenance candidate after Phases 143–151 and records the Phase 152 publication decision.

Phase 152 re-ran the final release gate and published only if safe. It created the annotated git tag and GitHub pre-release after checks passed. It did not create a package, binary artifact, Docker image, production deployment, write-mode default change, write-alpha expansion, or production-readiness claim.

## Checked revision

| Item | Result |
| --- | --- |
| Branch checked | `main` |
| Starting HEAD for Phase 151 | `83a0a3f47d0cbf727aa0634962ea0f29519a540e` |
| Starting `origin/main` for Phase 151 | `83a0a3f47d0cbf727aa0634962ea0f29519a540e` |
| Current public read-only release | `v0.1.4-readonly` |
| Candidate prepared | `v0.1.5-readonly` |
| Release phase starting HEAD | `a94a0a3` |
| Release phase starting `origin/main` | `a94a0a3` |
| Gate artifact commit note | Phase 152 changes release/status/handoff documentation only before publication; no backend, frontend, Docker, auth, or write-mode code was changed. |

## Required checks

| Area | Command / evidence | Result |
| --- | --- | --- |
| Starting state | `git status --short --branch` | PASS — `## main...origin/main` plus pre-existing untracked `.hermes/` local agent data, intentionally not committed |
| HEAD/origin parity before artifacts | `git rev-parse HEAD` and `git rev-parse origin/main` | PASS — both `83a0a3f` |
| Backend disabled-write targeted tests | `cd apps/api && pytest tests/test_health.py tests/test_transaction_writes.py -q` | PASS — `63 passed, 32 warnings` |
| Backend full tests | `cd apps/api && pytest -q` | PASS — `380 passed, 32 warnings` |
| Frontend static check | `cd apps/web && npm run check` | PASS — `svelte-check found 0 errors and 0 warnings` |
| Frontend auth-route checks | `cd apps/web && npm run test:auth-routes` | PASS — `auth route checks passed` |
| Frontend build | `cd apps/web && npm run build` | PASS — production build completed |
| Docker Compose config | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet` | PASS — command exited successfully |
| Write-disabled Compose default | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config | grep -n 'GNUCASH_WRITES_ENABLED'` | PASS — API and web both render `GNUCASH_WRITES_ENABLED: "false"` |
| Whitespace diff check | `git diff --check` | PASS — no output |
| Tag absence | `git tag --list 'v0.1.5-readonly'` | PASS — no local tag output |
| GitHub release absence | `gh release view v0.1.5-readonly --json tagName,url,isPrerelease,isDraft 2>&1 || true` | PASS — `release not found` |
| GitHub Actions state | `gh run list --branch main --limit 5 --json status,conclusion,headSha,workflowName,createdAt,url` and post-push `gh run watch` | PASS — starting HEAD CI passed, and the Phase 152 release commit CI was watched to success before publication of this docs-only phase |
| Sensitive tracked-file scan | Python scan over `git ls-files` with known synthetic/runtime-doc allowlist | PASS — `sensitive tracked-file hygiene scan passed` |
| Phase 152 pre-publish CI | GitHub Actions for the Phase 152 release commit | PASS — CI completed successfully before tag/release publication |

## Read-only and write-disabled safety confirmation

| Check | Result |
| --- | --- |
| Default write posture | PASS — release docs retain `GNUCASH_WRITES_ENABLED=false`; Phase 151 made no settings or code changes. |
| Disabled-write coverage | PASS — targeted disabled-write/config tests passed. |
| Controlled-write positioning | PASS — controlled writes remain post-MVP/experimental and are not marketed as safe for production books. |
| Product model | PASS — MVP model remains one installation, one local admin user, one default GnuCash book, read-only access. |
| Authoritative editor | PASS — release notes/checklist continue to state that GnuCash Desktop remains the authoritative editor. |

## Sensitive-data hygiene

Safe hygiene evidence only; no secret values or private user directories were inspected.

| Check | Result |
| --- | --- |
| Working tree before implementation | PASS — clean tracked tree on `main...origin/main`; untracked `.hermes/` local agent data was present and intentionally excluded from commit. |
| Changed-file scope | PASS — Phase 151 changes are release-prep/status/handoff documentation only. |
| Prohibited artifacts | PASS — final tracked-file hygiene scan found no unexpected tracked private-data or secret artifacts. |
| Auth token handling | PASS — `gh release view` / `gh run list` were used only for release/CI state; no token values were printed into docs. |

## Release state

`v0.1.5-readonly` was absent before Phase 152 publication:

```text
git tag --list 'v0.1.5-readonly'
# no output

gh release view v0.1.5-readonly
# release not found
```

After Phase 152 checks passed, the annotated tag and GitHub pre-release were created:

```text
git tag -a v0.1.5-readonly -m "v0.1.5-readonly"
git push origin v0.1.5-readonly
gh release create v0.1.5-readonly --title "v0.1.5-readonly" --notes-file docs/release/v0.1.5-readonly-notes.md --prerelease
```

Release URL: https://github.com/valentusys/gnucash-web-companion/releases/tag/v0.1.5-readonly

No package, uploaded binary artifact, Docker image, or production deployment was created.

## Verdict

`Published as authorized GitHub pre-release`.

Phase 152 satisfied the authorized release contract: clean `main`, `HEAD == origin/main`, tag/release absence, GitHub Actions success for the release HEAD, local checks, `GNUCASH_WRITES_ENABLED=false`, and sensitive tracked-file hygiene were re-confirmed before creating the tag/release.

## Remaining risks and non-claims

- Publication happened in Phase 152; `v0.1.5-readonly` is now the current public read-only pre-alpha release.
- Compatibility evidence remains intentionally narrow; no broad GnuCash version/backend compatibility claim is made.
- Phase 150 dogfood evidence is synthetic/disposable only and not broad real-book or production evidence.
- CSV export remains synchronous and capped at 10,000 rows.
- Controlled writes remain post-MVP/experimental and disabled by default.
- The project remains pre-alpha, not production-ready, and not security-audited.
