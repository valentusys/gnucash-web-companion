# v0.2.4-writealpha Final Release-Readiness Gate

Date: 2026-05-21
Phase: 211
Target release candidate: `v0.2.4-writealpha`
Verdict: `PASS — publish as conservative GitHub pre-release after exact release-commit CI succeeds`

## Scope

This gate compares current `main`, README/README.ru/PROJECT_STATUS/CHANGELOG, release artifacts, dogfood evidence, GitHub releases/actions/issues, and safety defaults after cycle-1 Phases 202–210.

The phase contract authorizes publication only if evidence warrants it and the exact release commit is green. Evidence warrants a write-alpha maintenance pre-release because cycle 1 materially changed or validated write-alpha/read-only/operator evidence after `v0.2.3-writealpha`: first-run diagnostics, compatibility evidence boundaries, multi-book recovery, transaction/scheduled read-only edge cases, audit-summary redaction, safety/localization copy, full default-read-only dogfood, and bounded write-alpha create/PATCH/DELETE/restore dogfood with default-false reset.

## Release target decision

- Selected target: `v0.2.4-writealpha`.
- Rationale: cycle 1 includes write-alpha/operator/read-only evidence and safety/readiness improvements beyond the published `v0.2.3-writealpha`; this is not read-only-only scope.
- Release type: GitHub pre-release only.
- Not published: packages, binary artifacts, Docker images, production deployment, public hosting, or any broader compatibility claim.

## Repository and GitHub state before release artifacts

| Item | Result |
| --- | --- |
| Branch | `main` |
| Starting HEAD | `f578a1a7aa3e32d21ed34516860ab1c78d03604c` |
| Starting `origin/main` | `f578a1a7aa3e32d21ed34516860ab1c78d03604c` |
| Working tree | clean tracked tree; only untracked `.hermes/` excluded |
| Current public read-only release | `v0.1.7-readonly` |
| Current public write-alpha release | `v0.2.3-writealpha` |
| Candidate local tag before publication | absent |
| Candidate remote tag before publication | absent |
| Candidate GitHub release before publication | absent |
| GitHub auth | available via `gh auth status` |
| GitHub Actions before release artifacts | Phase 210 push run was in progress during initial reconnaissance; exact release/status commit CI must pass before publication. |
| Open issues / known limitations | Existing future-work limitations remain non-blocking only because the release notes keep the non-claims explicit: no production/security/public-internet readiness, no broad Desktop/backend compatibility, and no real/private-book write safety. |

## Documentation/status comparison

| Area | Finding | Result |
| --- | --- | --- |
| `README.md` / `README.ru.md` | Public status stopped at Phase 201 / `v0.2.3-writealpha`. | Updated to Phase 211 and `v0.2.4-writealpha` publication status. |
| `PROJECT_STATUS.md` | Current baseline stopped at Phase 210. | Updated to completed Phase 211 and published `v0.2.4-writealpha`. |
| `CHANGELOG.md` | `Unreleased` listed Phases 204 and 206–210 only. | Updated with `0.2.4-writealpha` section for Phases 202–211. |
| Release docs | No `v0.2.4-writealpha` artifacts existed. | Added notes, checklist, final gate, and publication evidence. |
| Dogfood evidence | Phase 209–210 docs record synthetic/disposable-only evidence and default false resets. | Accepted with limits; no real/private-book write safety claim. |
| Safety wording | Must remain conservative. | Release notes/checklist/final gate deny production/security/real-private-book write safety claims. |

## Required local checks

| Area | Command / evidence | Result |
| --- | --- | --- |
| Working tree and parity | `git status --short --branch`; `git fetch origin main --tags --prune`; `git rev-parse HEAD origin/main` | PASS before release artifact edits. |
| Release absence | `git tag -l v0.2.4-writealpha`; `git ls-remote --tags origin refs/tags/v0.2.4-writealpha`; `gh release view v0.2.4-writealpha ...` | PASS — absent before publication. |
| `.env.example` default | `grep -n 'GNUCASH_WRITES_ENABLED' .env.example` | PASS — `GNUCASH_WRITES_ENABLED=false`. |
| Docker Compose config | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet` | PASS. |
| Rendered write default | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config \| grep -n 'GNUCASH_WRITES_ENABLED'` | PASS — API and web render `GNUCASH_WRITES_ENABLED: "false"`. |
| Backend suite | `cd apps/api && pytest -q` | PASS in Phase 211. |
| Frontend checks | `cd apps/web && npm run check && npm run test:auth-routes && npm run build` | PASS in Phase 211. |
| Whitespace diff check | `git diff --check` | PASS in Phase 211. |
| Sensitive tracked-file scan | Python scan over `git ls-files` with known synthetic/docs allowlist | PASS in Phase 211. |

## GitHub Actions gate

- Phase 210 `main` was the starting pushed baseline before the release/status commit.
- After the Phase 211 release/status commit is pushed, GitHub Actions for that exact commit must be watched to successful completion before tag/release publication.
- Publication is not allowed if the exact release/status commit CI fails or if tag/release absence checks change before tagging.

## Write-alpha safety confirmation

| Check | Result |
| --- | --- |
| Default write posture | PASS — `GNUCASH_WRITES_ENABLED=false` remains default in `.env.example` and rendered Compose. |
| Test-environment write gate | PASS — write execution still requires explicit enablement plus `APP_ENV=test`. |
| Write scope | PASS — release does not add or broaden create/PATCH/DELETE routes. |
| Evidence scope | PASS — new write evidence is synthetic/disposable only; Phase 210 completed bounded CRUD/restore evidence and reset to default false. |
| Product model | PASS — GnuCash Desktop remains authoritative; web app remains read-only by default. |
| Non-claims | PASS — release docs deny production readiness, security audit, broad compatibility, public-internet safety, and real/private/only-copy write safety. |

## Publication decision

Decision: publish `v0.2.4-writealpha` as a conservative GitHub pre-release after exact release-commit CI passes.

Rationale:

- A maintenance write-alpha pre-release is warranted by cycle-1 evidence and operator/readiness hardening after published `v0.2.3-writealpha`.
- Local release-critical checks passed.
- Tag/release were absent before publication.
- Safety defaults and write gates remain intact.
- The phase contract explicitly authorizes publication if the gate is green.

## Remaining risks and non-claims

- Write-alpha remains experimental, disabled by default, and executable only with `APP_ENV=test` when explicitly enabled.
- Real/private-book write safety is not established and not claimed.
- Compatibility evidence remains intentionally narrow; no broad GnuCash Desktop/backend compatibility claim is made.
- CSV export remains synchronous and capped at 10,000 rows.
- The project remains pre-alpha, not production-ready, and not security-audited.
