# v0.1.4-readonly Release-Prep Checklist

> Status: prepared in Phase 141 for a possible future `v0.1.4-readonly` maintenance pre-release.
>
> Publication status: unpublished / pending explicit authorization. Phase 141 must not create a git tag, GitHub release, package, or uploaded artifact.

## 1. Candidate scope

Status: prepared, not published.

- Release candidate name: `v0.1.4-readonly`.
- Release type: conservative pre-alpha read-only maintenance candidate after published `v0.1.3-readonly`.
- MVP baseline remains: one self-hosted installation, one local admin user, one default configured GnuCash book, read-only access.
- GnuCash Desktop remains the authoritative editor.
- Multi-book wording remains about independent accessible books, not collaborative accounting.
- Controlled-write/write-alpha code remains experimental post-MVP work and disabled by default.
- Required default posture remains:

```text
GNUCASH_WRITES_ENABLED=false
```

## 2. Candidate evidence from Phases 133–141

Meaningful read-only maintenance evidence exists after `v0.1.3-readonly`:

- Phase 133 — read-only empty/error states improved for books, scheduled awareness, transactions, accounts, and global user-safe error handling.
- Phase 134 — read-only loading skeletons added for dashboard, accounts, transactions, and books.
- Phase 135 — mobile navigation and small-screen transaction-detail split layout polished with touch-friendly controls.
- Phase 136 — compatibility documentation refreshed with explicit synthetic/disposable evidence boundaries and no broad Desktop-version claim.
- Phase 137 — local secure deployment guide refreshed for localhost/LAN/VPN CORS, JWT-secret handling, app metadata DB backup, and pre-deployment checks.
- Phase 138 — public README, Russian README, changelog, roadmap, project status, and handoff synchronized after recent maintenance work.
- Phase 139 — synthetic/disposable Docker/Caddy dogfood passed core read-only API/browser paths and disabled-write probes with `GNUCASH_WRITES_ENABLED=false`.
- Phase 140 — readiness audit completed with verdict `not ready` due to release/status documentation drift.
- Phase 141 — release notes, checklist, final gate, changelog/status updates, and handoff prepared to resolve that documentation blocker without publication.

## 3. Prepared artifacts

Prepared in Phase 141:

- `docs/release/v0.1.4-readonly-notes.md` — conservative release notes for a later authorized publish phase.
- `docs/release/v0.1.4-readonly-checklist.md` — this release-prep checklist.
- `docs/release/v0.1.4-readonly-final-gate.md` — release-gate verdict and verification evidence.
- `CHANGELOG.md` — records `v0.1.4-readonly` candidate scope and Phase 141 preparation without publication.
- `README.md` — points release-readiness links at the prepared current candidate while keeping the current public read-only release as `v0.1.3-readonly` until publication.
- `PROJECT_STATUS.md` — advances the baseline through Phase 141 and records candidate status.
- `docs/handoff/phase-141.md` — phase handoff and verification report.

## 4. Required checks for Phase 141 gate

Phase 141 records current results for:

```bash
cd apps/api && pytest tests/test_health.py tests/test_auth.py -q
cd apps/web && npm run check
cd apps/web && npm run test:auth-routes
JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet
git diff --check
git tag --list 'v0.1.4-readonly'
gh release view v0.1.4-readonly || true
```

Before any later publish phase, re-run the full release-state and safety checks immediately before creating a tag/release.

## 5. Publication steps reserved for a later explicitly authorized phase

Do not run these in Phase 141. They are documented only so a later authorized release executor can copy them after re-checking the gate:

```bash
git tag -a v0.1.4-readonly -m "v0.1.4-readonly"
git push origin v0.1.4-readonly
gh release create v0.1.4-readonly \
  --title "v0.1.4-readonly" \
  --notes-file docs/release/v0.1.4-readonly-notes.md \
  --prerelease
```

Required preconditions before those commands in a future phase:

- explicit Val authorization to publish `v0.1.4-readonly`;
- clean `main` and `HEAD == origin/main`;
- recent GitHub Actions for `main` are successful or any blocker is documented and accepted;
- no existing local tag named `v0.1.4-readonly`;
- no existing GitHub release named `v0.1.4-readonly`;
- final gate still says ready;
- no real/private financial data or secrets in tracked files;
- `GNUCASH_WRITES_ENABLED=false` remains the default.

## 6. Safety checklist

Before any future publication, re-confirm:

- `GNUCASH_WRITES_ENABLED=false` remains the default.
- Release notes say pre-alpha and read-only by default.
- Release notes say not production-ready and not security-audited.
- Release notes tell users to test disposable/copy books first and keep backups.
- Release notes do not claim broad GnuCash compatibility.
- Release notes do not claim production-safe write mode.
- Release notes do not overstate synthetic/dogfood evidence.
- No tag/release is created until the publish phase is explicitly authorized.
- No real/private financial data, GnuCash books, `.env`, app DBs, backups, screenshots, exports, secrets, tokens, certs, or keys are committed.

## 7. Known limitations and open risks

- Compatibility evidence remains intentionally narrow; future real Desktop-generated synthetic SQLite fixture coverage is still useful.
- CSV export remains synchronous and capped at 10,000 rows.
- Russian localization remains incomplete and English remains canonical.
- Controlled writes remain post-MVP/experimental and disabled by default; no write-mode feature should be release-marketed as safe for production books.
- Phase 139 dogfood evidence is synthetic/disposable only and must not be presented as production or broad real-book evidence.
