# v0.1.4-readonly Publication Evidence

Date: 2026-05-19
Phase: 142
Target release: `v0.1.4-readonly`
Verdict: `Published as authorized GitHub pre-release`

## Authorization and scope

Phase 142 was executed under the user instruction to run the release/publication gate for roadmap phase 10 / project phase 142 and to publish only if the gate was safe and the roadmap required publication. The gate was safe, so the phase published the prepared `v0.1.4-readonly` read-only maintenance pre-release.

Publication scope was limited to:

- annotated git tag `v0.1.4-readonly`;
- GitHub pre-release `v0.1.4-readonly`;
- release/status documentation synchronized in this repository.

No package, binary artifact, Docker image publication, product-code change, write-alpha expansion, runtime default change, or real/private data artifact was included.

## Final pre-publication gate

The final gate was re-run on `main` immediately before publication.

| Check | Result |
| --- | --- |
| Branch | PASS — `main` |
| Clean tracked tree | PASS — `git status --short --branch` showed `## main...origin/main` plus pre-existing untracked `.hermes/` local agent data only |
| HEAD/origin parity | PASS — `HEAD == origin/main` before release docs update and again after the Phase 142 commit was pushed and CI passed |
| Candidate artifacts present | PASS — release notes, checklist, final gate, and this publication evidence document are present |
| Existing tag/release absence | PASS before publication — no local tag or GitHub release named `v0.1.4-readonly` existed |
| GitHub Actions | PASS — latest five `main` CI runs were `completed success` before publication, including the Phase 142 documentation commit |
| Backend disabled-write test | PASS — `cd apps/api && pytest tests/test_transaction_writes.py::TestWritesDisabledByDefault -q` returned `8 passed, 1 warning` |
| Frontend check | PASS — `cd apps/web && npm run check` returned `svelte-check found 0 errors and 0 warnings` |
| Frontend auth-route checks | PASS — `cd apps/web && npm run test:auth-routes` returned `auth route checks passed` |
| Docker Compose config | PASS — `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet` exited successfully |
| Write-disabled default | PASS — rendered Compose config includes `GNUCASH_WRITES_ENABLED: "false"` for API and web services |
| Whitespace diff check | PASS — `git diff --check` exited successfully |
| Sensitive tracked-file hygiene | PASS — refined tracked-path scan found no unexpected tracked `.env`, app DB, runtime DB, private book, backup artifact, secret/token/cert/key, CSV export artifact, or private media artifact; existing allowlisted fixtures/placeholders/docs images were not new phase artifacts |

## Published artifacts

- Release commit: Phase 142 commit / tag target (see `git rev-parse v0.1.4-readonly^{}` after publication).
- Git tag: `v0.1.4-readonly`
- GitHub release URL: <https://github.com/valentusys/gnucash-web-companion/releases/tag/v0.1.4-readonly>
- Release type: GitHub pre-release
- Release notes source: `docs/release/v0.1.4-readonly-notes.md`

## Safety notes

- `GNUCASH_WRITES_ENABLED=false` remains the default.
- Controlled-write/write-alpha code remains experimental, post-MVP, disabled by default, and outside this read-only maintenance release scope.
- The project remains pre-alpha, not production-ready, and not security-audited.
- Users must test with disposable fixtures or copied GnuCash SQL books first and keep backups.
- No real/private GnuCash books, app DBs, backups, `.env`, tokens, keys, certs, screenshots, exports, private paths, or real/private financial data were added or committed in this publication phase.
