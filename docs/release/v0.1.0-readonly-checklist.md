# v0.1.0-readonly Release Checklist

> Status: final gate completed in Phase 79. This document records readiness evidence only; it does not publish a tag or GitHub release. Publication remains a separate explicit step.

## 1. Scope confirmation

- [x] Release name is `v0.1.0-readonly`.
- [x] Release is described as read-only by default.
- [x] GnuCash Desktop is described as the authoritative editor.
- [x] The MVP baseline is one installation, one local admin user, and one default configured GnuCash book.
- [x] Any multi-book language is framed as independent accessible books, not collaborative accounting.
- [x] Controlled-write code is described as experimental post-MVP and disabled by default.
- [x] `GNUCASH_WRITES_ENABLED=false` remains the documented/configured default.

## 2. Explicit non-goals verified

- [x] No default-enabled writes.
- [x] No new write capability added for this release.
- [x] No account/invoice/bill/customer/vendor/scheduled-transaction editing.
- [x] No CSV/OFX import.
- [x] No banking integration.
- [x] No hosted SaaS positioning.
- [x] No collaborative accounting or family-wallet baseline positioning.
- [x] No production-readiness or audited-security claim.
- [x] No broad GnuCash-version compatibility guarantee.

## 3. Documentation readiness

- [x] README current status matches the intended v0.1 release posture.
- [x] CHANGELOG has a release-facing Unreleased summary.
- [x] Release notes for `v0.1.0-readonly` exist and are conservative: `docs/release/v0.1.0-readonly-notes.md`.
- [x] `docs/release/v0.1.0-readonly-plan.md` is still accurate for the conservative read-only scope.
- [x] This checklist is complete or has explicit accepted exceptions.
- [x] Safety docs still tell users to test copied/disposable books first.
- [x] Deployment docs warn against direct public-internet exposure.
- [x] Troubleshooting docs avoid asking users to share secrets or real financial data.
- [x] Compatibility docs and release notes clearly limit what has been validated.

## 4. Automated checks

Run from repository root unless noted.

- [x] Backend tests pass:

```bash
cd apps/api && pytest -q
```

Phase 79 result: `282 passed, 27 warnings`.

- [x] Disabled-write safety is covered by the full backend suite and Phase 78 runtime probes:

```text
validate/create/patch write probes returned 403 with GNUCASH_WRITES_ENABLED=false
```

- [x] Frontend check passes:

```bash
cd apps/web && npm run check
```

Phase 79 result: `svelte-check found 0 errors and 0 warnings`.

- [x] Frontend auth-route/static safety checks pass:

```bash
cd apps/web && npm run test:auth-routes
```

Phase 79 result: `auth route checks passed`.

- [x] Frontend production build passes:

```bash
cd apps/web && npm run build
```

Phase 79 result: build completed successfully.

- [x] Docker Compose config validates:

```bash
JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet
```

Phase 79 result: passed.

- [x] Whitespace diff check passes:

```bash
git diff --check
```

Phase 79 result: passed.

## 5. Runtime smoke / dogfood gate

Use only copied or disposable GnuCash data.

- [x] Clean deployment/runtime directory used for dogfood evidence.
- [x] `.env` created locally and not committed.
- [x] `JWT_SECRET` was locally set for runtime dogfood and not committed.
- [x] Admin password/hash was locally set and not committed.
- [x] `GNUCASH_WRITES_ENABLED=false` confirmed.
- [x] Disposable/copied SQL book placed under runtime `data/books/` outside git.
- [x] Docker Compose started successfully.
- [x] `/api/health` returned non-sensitive health data.
- [x] Automated smoke script passed:

```bash
SMOKE_API_BASE_URL=http://127.0.0.1:18080/api \
SMOKE_ADMIN_PASSWORD='<redacted>' \
scripts/smoke/read-only-api-smoke.py
```

Phase 78 result: `PASS: read-only API smoke checks completed`.

- [x] Manual login works.
- [x] Dashboard loads.
- [x] Accounts tree loads.
- [x] Account detail loads.
- [x] Transactions list loads.
- [x] Transaction detail loads.
- [x] CSV export downloads expected copied/disposable-book data.
- [x] Write entry points are hidden or blocked while writes are disabled.
- [x] No screenshots/exports from real data are committed.

Accepted exception/limitation: the dogfood book was a copied synthetic/disposable SQL fixture, not an explicitly provided real personal copied book. Phase 79 accepts this for v0.1 because #25 allowed copied or disposable data and the release notes require every user to test a copied book first.

## 6. Compatibility gate

- [x] Synthetic fixture tests pass as part of the backend suite.
- [x] Compatibility docs name the tested SQLite fixture scope.
- [x] Release notes do not claim PostgreSQL/MySQL/MariaDB/XML support.
- [x] Release notes do not claim support for all GnuCash Desktop versions.
- [x] Issue #22 is explicitly kept open as a non-blocking broader compatibility follow-up.

## 7. Security/sensitive-data hygiene

- [x] `.env` is not tracked.
- [x] App metadata DB files are not tracked.
- [x] Real GnuCash books are not tracked.
- [x] Backups are not tracked.
- [x] Secrets, tokens, keys, certs, and credentials are not tracked.
- [x] Real screenshots are not tracked.
- [x] Real CSV/financial exports are not tracked.
- [x] Auth token storage remains httpOnly-cookie based, not localStorage/sessionStorage.
- [x] No telemetry was added.

## 8. GitHub/project hygiene

- [x] Open issues were reviewed before release gate.
- [x] Issues #11, #12, #13, #17, and #22 remain explicit v0.1 out-of-scope/non-blocking items under the Phase 56 release plan.
- [x] Final release-gate artifact exists: `docs/release/v0.1.0-readonly-final-gate.md`.
- [x] Required local checks and GitHub Actions CI are green for the release-gate commit.
- [x] Release-gate commit has a clean working tree before tagging/publication.

Issue decisions in Phase 79:

- #24 closed: conservative release notes completed.
- #25 closed: Phase 78 copied/disposable-data runtime dogfood evidence accepted.
- #37 verified closed/fixed.

## 9. Tag/release publication gate

Do not run these commands until a later explicit release phase says to publish and every required gate remains satisfied:

```bash
git tag -a v0.1.0-readonly -m "v0.1.0-readonly"
git push origin v0.1.0-readonly
gh release create v0.1.0-readonly \
  --title "v0.1.0-readonly" \
  --notes-file docs/release/v0.1.0-readonly-notes.md \
  --prerelease
```

Phase 79 verification:

```text
git tag -l 'v0.1*'
(no output)

gh release view v0.1.0-readonly
not found / no release published
```

## 10. Rollback readiness

- [x] Previous known-good release/tag is identified: `v0.0.2-prealpha`.
- [x] App metadata DB backup requirement is documented for tested deployments.
- [x] Copied/disposable book backup requirement is documented for tested deployments.
- [x] Rollback steps in `docs/release/v0.1.0-readonly-plan.md` are reviewed.
- [x] Failed release/test deployments will remain private until fixed.

## Phase 79 final gate result

`v0.1.0-readonly` is ready for publication as a separate explicit next step. Phase 79 created release notes, accepted dogfood evidence, closed #24/#25, verified #37 closed, passed required checks, and did not publish a tag or release.
