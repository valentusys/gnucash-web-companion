# v0.4.0-owner-writebeta PM decision

Date: 2026-06-04

## Decision

`NO_RELEASE_KEEP_MAINTENANCE`.

Do not publish, tag, package, image, or claim `v0.4.0-owner-writebeta` from this run.

## Rationale

#22 is now reconciled and closed narrowly, which removes the docs-vs-GitHub drift. However, a `v0.4.0-owner-writebeta` GitHub prerelease would still risk overclaiming the controlled-write evidence because:

- #36 remains open.
- W3 copied-book dogfood is accepted only for one staged outside-git copied/restorable target.
- Supported-version write compatibility remains unaccepted beyond narrow synthetic/copied evidence.
- Real/private/original/working/only-copy mutation remains unauthorized and unsafe as a first-use claim.
- Public write beta, production, stable, security-audited, public-internet-safe, and broad compatibility claims remain blocked.

## Allowed current posture

- Continue maintenance and guard/docs tightening.
- Keep `v0.5.0-public-readonly-beta` as the current public read-only beta.
- Keep existing write-alpha/writebeta code disabled by default with `GNUCASH_WRITES_ENABLED=false`.
- Keep enabled write-alpha/writebeta execution `APP_ENV=test` gated.
- Keep W3 evidence described only as staged copied/restorable evidence.

## Not authorized

- No `v0.4.0-owner-writebeta` release.
- No public write beta.
- No real/private/original/working/only-copy mutation.
- No release/tag/package/image publication.
- No production/stable/security-audited claim.

## Next recommended prompt

"Continue #36 with a PM closure packet only: decide whether the original controlled-write remaining-gates issue can be closed without any release, preserving owner-only copied/restorable scope, no real-book safety claim, and no public write beta. Do not run dogfood unless a new copied/restorable target and exact same-context authorization are provided."
