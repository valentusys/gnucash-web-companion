# v0.1.2-readonly Final Release Gate

Date: 2026-05-19
Phase: 115
Target release candidate: `v0.1.2-readonly`
Verdict: `Ready for later authorized publish phase`

## Scope

This release gate verifies the prepared `v0.1.2-readonly` read-only maintenance candidate after Phases 106–114 and the Phase 115 release notes/checklist.

This phase does not publish a git tag, create a GitHub release, upload packages, enable writes, expand controlled-write scope, or claim production readiness.

## Checked revision

| Item | Result |
| --- | --- |
| Branch checked | `main` |
| HEAD checked before Phase 115 artifacts | `e2ce16947a172fe36dad028998687767ac1198a7` |
| Current public release before this phase | `v0.1.1-readonly` |
| Candidate prepared | `v0.1.2-readonly` |
| Gate artifact commit note | The checks below were run during Phase 115. The phase changed only release/status/handoff documentation; no backend, frontend, Docker, auth, or write-mode code was changed. |

## Required checks

| Area | Command / evidence | Result |
| --- | --- | --- |
| Backend test suite | `cd apps/api && pytest -q` | PASS — `349 passed, 27 warnings in 131.85s` |
| Frontend static check | `cd apps/web && npm run check` | PASS — `svelte-check found 0 errors and 0 warnings` |
| Frontend auth-route checks | `cd apps/web && npm run test:auth-routes` | PASS — `auth route checks passed` |
| Frontend production build | `cd apps/web && npm run build` | PASS — SvelteKit/Vite build completed successfully |
| Docker Compose config | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet` | PASS — command exited successfully with no output |
| Whitespace diff check | `git diff --check` | PASS — no output after final handoff/status edits |
| GitHub auth | `gh auth status` | PASS — authenticated as `valentusys` |
| GitHub Actions recent `main` state | `gh run list --limit 10 --json status,conclusion,headBranch,displayTitle,url` | PASS — latest 10 listed `main` runs were completed/success, through `Add synthetic browser dogfood refresh` |
| Tag absence | `git tag --list 'v0.1.2-readonly'` | PASS — no local tag output |
| GitHub release absence | `gh release view v0.1.2-readonly || true` | PASS — `release not found` |
| Sensitive tracked-file scan | Python scan over `git ls-files` with known synthetic fixture allowlist | PASS — no unexpected tracked sensitive artifact names |

## Read-only and write-disabled safety confirmation

| Check | Result |
| --- | --- |
| Default write posture | PASS — `.env.example`, backend settings, and release docs retain `GNUCASH_WRITES_ENABLED=false` / `gnucash_writes_enabled: bool = False` as the default posture. |
| Disabled-write coverage | PASS — backend full test suite includes existing disabled-write route regression coverage; Phase 115 did not enable writes. |
| Controlled-write positioning | PASS — controlled writes remain post-MVP/experimental and are not marketed as safe for production books. |
| Product model | PASS — MVP model remains one installation, one local admin user, one default GnuCash book, read-only access. |
| Authoritative editor | PASS — release notes/checklist continue to state that GnuCash Desktop remains the authoritative editor. |

## Sensitive-data hygiene

Safe hygiene evidence only; no secret values or private user directories were inspected.

| Check | Result |
| --- | --- |
| Working tree before implementation | PASS — `git status --short` was clean before Phase 115 docs were created. |
| Changed-file scope | PASS — Phase 115 changes are release-prep/status/handoff documentation only. |
| Prohibited artifacts | PASS — no real/private financial data, GnuCash books, app DBs, backups, `.env`, secrets, tokens, certs, keys, screenshots, or private CSV exports were created or added for this phase. |
| Auth token handling | PASS — `gh auth status` redacted token output; no token values were copied into committed files. |

## Release state

`v0.1.2-readonly` remains unpublished after this gate:

```text
git tag --list 'v0.1.2-readonly'
(no output)

gh release view v0.1.2-readonly
release not found
```

No tag or GitHub release was created in Phase 115.

## Verdict

`Ready for later authorized publish phase`.

All required Phase 115 release-gate checks passed. A future publish phase may create `v0.1.2-readonly` only after separate explicit Val authorization and immediate re-check of tag/release absence.

## Remaining risks and non-claims

- GitHub #38 remains open/blocked; no personal copied-book dogfood success is claimed for `v0.1.2-readonly`.
- Compatibility evidence remains intentionally narrow; no broad GnuCash version/backend compatibility claim is made.
- CSV export remains synchronous and capped at 10,000 rows.
- Controlled writes remain post-MVP/experimental and disabled by default.
- The project remains pre-alpha, not production-ready, and not security-audited.
