# v0.2.5-writealpha Release Notes — not published

Date: 2026-05-21
Phase: 221
Target candidate: `v0.2.5-writealpha`
Publication status: `NOT PUBLISHED`

## No-release summary

`v0.2.5-writealpha` was evaluated as a possible conservative write-alpha GitHub pre-release for cycle 2, but it was not published.

The blocking reason is the Phase 220 bounded write-alpha DELETE backup-count anomaly: after a successful synthetic/disposable DELETE route execution, redacted inspection showed successful create, PATCH, and DELETE backup-bearing audit rows but fewer backup files than expected. Because this cycle requires bounded create/PATCH/DELETE write-alpha evidence before publication, the gate fails until the anomaly is investigated and the write-alpha route-family smoke is rerun cleanly.

No tag, GitHub release, package, binary artifact, Docker image, production deployment, or write-default change was created for `v0.2.5-writealpha`.

Phase 229 refreshed the public status/release-doc drift guard after Phases 222–228 remediated and smoke-verified the blocker closure in synthetic/disposable scope. The current published write-alpha pre-release remains `v0.2.4-writealpha` until a later authorized release phase succeeds.

## Accepted cycle-2 evidence

Cycle 2 produced meaningful non-release changes and evidence after `v0.2.4-writealpha`:

- public status/roadmap drift guard for current release posture;
- tagged fresh-clone smoke for `v0.2.4-writealpha` with default-disabled writes;
- synthetic upgrade smoke from `v0.2.4-writealpha` runtime state to current `main`;
- unavailable-book read-only error-contract hardening;
- CSV/list parity regressions and URL-only/mobile export safety checks;
- write-alpha backup-failure/no-mutation tests;
- write-alpha audit-summary pagination/review UX;
- EN/RU operator-safety/accounting glossary slice;
- full default-read-only API/browser dogfood in Phase 220.

Those changes remain useful on `main`, but the write-alpha release gate is blocked by the DELETE backup-count evidence gap.

## Safety posture

- `GNUCASH_WRITES_ENABLED=false` remains the default.
- Explicit write-alpha execution remains constrained to local `APP_ENV=test` plus `GNUCASH_WRITES_ENABLED=true` on synthetic/disposable or copied test books only.
- No real/private-book or only-copy write safety is claimed.
- The project remains pre-alpha, not production-ready, and not security-audited.
- No broad GnuCash Desktop/backend compatibility, hosted SaaS readiness, public-internet safety, collaborative accounting, CSV/OFX import, banking integration, or production-safe write mode is claimed.

## Next safe engineering phase

Before any later publication, rerun the release gate on current `main` after fresh bounded evidence. Do not publish `v0.2.5-writealpha` from these Phase 221 notes; they are retained as historical no-release release-support docs.
