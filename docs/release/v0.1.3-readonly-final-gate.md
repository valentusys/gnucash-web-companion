# v0.1.3-readonly Final Release Gate

Date: 2026-05-19
Phase: 122
Target release candidate: `v0.1.3-readonly`
Verdict: `Ready for later authorized publish phase — unpublished`

## Scope

This release gate verifies the prepared `v0.1.3-readonly` read-only maintenance candidate after Phases 118–121 and the Phase 122 full read-only stability check.

Phase 122 does not publish a git tag, create a GitHub release, upload packages, enable writes, expand controlled-write scope, or claim production readiness. Publication remains pending separate explicit authorization from Val.

## Checked revision

| Item | Result |
| --- | --- |
| Branch checked | `main` |
| HEAD checked before Phase 122 artifacts | `5eefc8b93b54ea238b07fa2da5e9442eb0959c6b` |
| Current public release before this phase | `v0.1.2-readonly` |
| Candidate prepared | `v0.1.3-readonly` |
| Gate artifact commit note | The checks below were run during Phase 122. The phase changed release/status/handoff documentation only; no backend, frontend, Docker, auth, or write-mode code was changed. |

## Required checks

| Area | Command / evidence | Result |
| --- | --- | --- |
| Backend test suite | `cd apps/api && pytest -q` | PASS — `353 passed, 27 warnings in 132.62s` |
| Frontend static check | `cd apps/web && npm run check` | PASS — `svelte-check found 0 errors and 0 warnings` |
| Frontend auth-route checks | `cd apps/web && npm run test:auth-routes` | PASS — `auth route checks passed` |
| Frontend production build | `cd apps/web && npm run build` | PASS — SvelteKit/Vite build completed successfully |
| Docker Compose config | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet` | PASS — command exited successfully with no output |
| Whitespace diff check | `git diff --check` | PASS — no output |
| GitHub auth | `gh auth status` | PASS — authenticated as `valentusys`; token value redacted by `gh` output |
| GitHub Actions recent `main` state | `gh run list --limit 10 --json status,conclusion,headBranch,displayTitle,url` | PASS — latest 10 listed `main` runs were completed/success, through `Fix dashboard summary zero fallback` |
| Tag absence | `git tag --list 'v0.1.3-readonly'` | PASS — no local tag output |
| GitHub release absence | `gh release view v0.1.3-readonly --json tagName,url,isPrerelease,isDraft 2>&1 || true` | PASS — `release not found` |
| Sensitive tracked-file scan | Python scan over `git ls-files` with known synthetic fixture allowlist | PASS — no unexpected sensitive-looking tracked files outside synthetic fixture allowlist; allowed synthetic fixtures were `apps/api/tests/fixtures/test-book.gnucash.sqlite` and `apps/api/tests/fixtures/test-book-multicurrency.gnucash.sqlite` |

## Recent GitHub Actions evidence

Latest 10 `main` runs reported by `gh run list --limit 10 --json status,conclusion,headBranch,displayTitle,url` were all `completed/success`:

- `Fix dashboard summary zero fallback` — https://github.com/valentusys/gnucash-web-companion/actions/runs/26070870232
- `perf: optimize account detail transactions` — https://github.com/valentusys/gnucash-web-companion/actions/runs/26070547768
- `fix: strip root account display prefix` — https://github.com/valentusys/gnucash-web-companion/actions/runs/26069804577
- `fix: stabilize transaction table columns` — https://github.com/valentusys/gnucash-web-companion/actions/runs/26069459416
- `docs: publish v0.1.2-readonly release` — https://github.com/valentusys/gnucash-web-companion/actions/runs/26067690574
- `docs: record phase 116 personal-book dogfood` — https://github.com/valentusys/gnucash-web-companion/actions/runs/26067403357
- `Prepare v0.1.2-readonly release gate` — https://github.com/valentusys/gnucash-web-companion/actions/runs/26066225186
- `Add synthetic browser dogfood refresh` — https://github.com/valentusys/gnucash-web-companion/actions/runs/26065918409
- `Add Russian localization glossary and transaction UI slice` — https://github.com/valentusys/gnucash-web-companion/actions/runs/26065330945
- `Add CORS deployment posture diagnostics` — https://github.com/valentusys/gnucash-web-companion/actions/runs/26064835073

## Read-only and write-disabled safety confirmation

| Check | Result |
| --- | --- |
| Default write posture | PASS — release docs retain `GNUCASH_WRITES_ENABLED=false`; Phase 122 made no settings or code changes. |
| Disabled-write coverage | PASS — backend full test suite includes existing disabled-write route regression coverage; Phase 122 did not enable writes. |
| Controlled-write positioning | PASS — controlled writes remain post-MVP/experimental and are not marketed as safe for production books. |
| Product model | PASS — MVP model remains one installation, one local admin user, one default GnuCash book, read-only access. |
| Authoritative editor | PASS — release notes/checklist continue to state that GnuCash Desktop remains the authoritative editor. |

## Sensitive-data hygiene

Safe hygiene evidence only; no secret values or private user directories were inspected.

| Check | Result |
| --- | --- |
| Working tree before implementation | PASS — `git status --short --branch` was clean on `main...origin/main` before Phase 122 docs were created. |
| Changed-file scope | PASS — Phase 122 changes are release-prep/status/handoff documentation only. |
| Prohibited artifacts | PASS — no real/private financial data, GnuCash books, app DBs, backups, `.env`, secrets, tokens, certs, keys, screenshots, or private CSV exports were created or added for this phase. |
| Auth token handling | PASS — `gh auth status` redacted the token output; no token values were copied into committed files. |

## Release state

`v0.1.3-readonly` remains unpublished after the Phase 122 gate:

```text
git tag --list 'v0.1.3-readonly'
(no output)

gh release view v0.1.3-readonly
release not found
```

No tag, GitHub release, package, or uploaded artifact was created in Phase 122.

## Verdict

`Ready for later authorized publish phase — unpublished`.

All required Phase 122 release-gate checks passed. A later publish phase may create `v0.1.3-readonly` only after explicit Val authorization and immediate re-check of clean tree, HEAD/origin parity, tag/release absence, GitHub Actions success, local checks, and sensitive tracked-file hygiene.

## Remaining risks and non-claims

- Compatibility evidence remains intentionally narrow; no broad GnuCash version/backend compatibility claim is made.
- CSV export remains synchronous and capped at 10,000 rows.
- Phase 120 performance evidence is local/synthetic and not a production scalability guarantee.
- Controlled writes remain post-MVP/experimental and disabled by default.
- The project remains pre-alpha, not production-ready, and not security-audited.
