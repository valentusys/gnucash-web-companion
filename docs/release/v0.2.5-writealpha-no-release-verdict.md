# v0.2.5-writealpha Historical No-Release Verdict — superseded

Date: 2026-05-21
Original phase: 221
Superseding phase: 231
Status: `SUPERSEDED BY v0.2.5-writealpha PUBLICATION`

## Original decision

Phase 221 did not publish `v0.2.5-writealpha` because Phase 220 exposed a bounded synthetic/disposable write-alpha DELETE backup-count anomaly after successful route execution.

The original blocker was valid at the time: a write-alpha maintenance release required matching create/PATCH/DELETE backup/audit evidence, and Phase 220 showed more successful backup-bearing audit rows than readable backup artifacts.

## Superseding evidence

Phases 222–230 later remediated and re-verified the blocker in narrow synthetic/disposable scope:

1. Phase 222 found and fixed backup artifact collision/overwrite risk from second-precision names plus overwrite-capable copy.
2. Phase 223 hardened backup identity and audit correlation with deterministic tests.
3. Phase 224 passed DELETE restore proof v2.
4. Phase 225 passed create/PATCH/DELETE backup-audit matrix evidence.
5. Phase 226 rechecked the default read-only path with `GNUCASH_WRITES_ENABLED=false`.
6. Phase 227 updated operator-facing wording to describe blocker closure narrowly.
7. Phase 228 passed current-HEAD fresh-clone and `v0.2.4-writealpha` upgrade smokes with disabled writes.
8. Phase 229 refreshed public status/release-doc drift guards.
9. Phase 230 passed the final release-candidate dogfood pack.
10. Phase 231 reran the final release gate and published `v0.2.5-writealpha` only after exact release/status commit CI passed.

## Current decision

`v0.2.5-writealpha` is now published as a conservative GitHub pre-release by Phase 231.

The historical Phase 221 no-release decision remains documented for auditability, but it is no longer the current release posture.

## Safety boundaries retained

- `GNUCASH_WRITES_ENABLED=false` remains default.
- `APP_ENV=test` remains required for explicit write-alpha execution.
- Write-alpha evidence remains synthetic/disposable or copied-test-book only.
- No real/private-book or only-copy write safety is claimed.
- The project remains pre-alpha, not production-ready, not stable, not security-audited, and not public-internet safe.
