# v0.1.4-readonly Final Release Gate

Date: 2026-05-19
Phase: 142
Target release candidate: `v0.1.4-readonly`
Verdict: `Passed — published as authorized GitHub pre-release`

## Scope

This release gate verifies the prepared `v0.1.4-readonly` read-only maintenance candidate after Phases 133–142.

Phase 142 re-ran the publication gate and, after the gate passed under explicit publish authorization, created the annotated git tag and GitHub pre-release. No package, binary artifact, Docker image, production deployment, write-mode default change, write-alpha expansion, or production-readiness claim was added.

## Checked revision

| Item | Result |
| --- | --- |
| Branch checked | `main` |
| HEAD checked before Phase 141 artifacts | `7699ce399ebce3aa5b48ef293960fa759badca95` |
| `origin/main` before Phase 141 artifacts | `7699ce399ebce3aa5b48ef293960fa759badca95` |
| Current public read-only release before Phase 142 | `v0.1.3-readonly` |
| Published release | `v0.1.4-readonly` |
| Gate artifact commit note | The checks below were re-run during Phase 142. The phase changed release/status/handoff documentation only before publishing the tag/release; no backend, frontend, Docker, auth, or write-mode code was changed. |

## Required checks

| Area | Command / evidence | Result |
| --- | --- | --- |
| Starting state | `git status --short --branch` | PASS — `## main...origin/main` plus pre-existing untracked `.hermes/` local agent data, intentionally not committed |
| HEAD/origin parity | `git rev-parse HEAD` and `git rev-parse origin/main` | PASS before Phase 141 artifacts — both `7699ce399ebce3aa5b48ef293960fa759badca95` |
| Backend targeted tests | `cd apps/api && pytest tests/test_health.py tests/test_auth.py -q` | PASS — `17 passed, 1 warning in 4.94s` |
| Frontend static check | `cd apps/web && npm run check` | PASS — `svelte-check found 0 errors and 0 warnings` |
| Frontend auth-route checks | `cd apps/web && npm run test:auth-routes` | PASS — `auth route checks passed` |
| Docker Compose config | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet` | PASS — command exited successfully with no output |
| Whitespace diff check | `git diff --check` | PASS — no output |
| Tag absence | `git tag --list 'v0.1.4-readonly'` | PASS — no local tag output |
| GitHub release absence | `gh release view v0.1.4-readonly --json tagName,url,isPrerelease,isDraft 2>&1 || true` | PASS — `release not found` |
| Sensitive tracked-file scan | Python scan over `git ls-files` with known synthetic/runtime-doc allowlist | PASS — no unexpected tracked `.env`, app DB, runtime DB, private book, backup artifact, secret/token/cert/key, CSV export artifact, or private media artifact paths; allowlist covers `.env.example`, `data/app/.gitkeep`, and existing synthetic fixture books |

## Read-only and write-disabled safety confirmation

| Check | Result |
| --- | --- |
| Default write posture | PASS — release docs retain `GNUCASH_WRITES_ENABLED=false`; Phase 141 made no settings or code changes. |
| Disabled-write coverage | PASS — targeted backend health/auth checks and unchanged existing disabled-write coverage remain in the project; Phase 141 did not enable writes. |
| Controlled-write positioning | PASS — controlled writes remain post-MVP/experimental and are not marketed as safe for production books. |
| Product model | PASS — MVP model remains one installation, one local admin user, one default GnuCash book, read-only access. |
| Authoritative editor | PASS — release notes/checklist continue to state that GnuCash Desktop remains the authoritative editor. |

## Sensitive-data hygiene

Safe hygiene evidence only; no secret values or private user directories were inspected.

| Check | Result |
| --- | --- |
| Working tree before implementation | PASS — clean tracked tree on `main...origin/main`; untracked `.hermes/` local agent data was present and intentionally excluded from commit. |
| Changed-file scope | PASS — Phase 141 changes are release-prep/status/handoff documentation only. |
| Prohibited artifacts | PASS — no real/private financial data, GnuCash books, app DBs, backups, `.env`, secrets, tokens, certs, keys, screenshots, CSV exports, package uploads, or private media were created or added for this phase. |
| Auth token handling | PASS — `gh release view` was used only for release absence; no token values were printed into docs. |

## Release state

`v0.1.4-readonly` was unpublished after the Phase 141 gate and published after the Phase 142 gate:

```text
git tag --list 'v0.1.4-readonly'
v0.1.4-readonly

gh release view v0.1.4-readonly --json tagName,url,isPrerelease,isDraft
{"isDraft":false,"isPrerelease":true,"tagName":"v0.1.4-readonly","url":"https://github.com/valentusys/gnucash-web-companion/releases/tag/v0.1.4-readonly"}
```

No package, uploaded binary artifact, Docker image, production deployment, or publication beyond the tag/GitHub pre-release was created in Phase 142.

## Verdict

`Passed — published as authorized GitHub pre-release`.

All required Phase 142 release-gate checks passed. `v0.1.4-readonly` was published only after explicit authorization and immediate re-check of clean tree, HEAD/origin parity, tag/release absence, GitHub Actions success, local checks, `GNUCASH_WRITES_ENABLED=false`, and sensitive tracked-file hygiene.

## Remaining risks and non-claims

- Publication created only the annotated git tag and GitHub pre-release; no packages or binary artifacts were published.
- Compatibility evidence remains intentionally narrow; no broad GnuCash version/backend compatibility claim is made.
- Phase 139 dogfood evidence is synthetic/disposable only and not broad real-book or production evidence.
- CSV export remains synchronous and capped at 10,000 rows.
- Controlled writes remain post-MVP/experimental and disabled by default.
- The project remains pre-alpha, not production-ready, and not security-audited.
