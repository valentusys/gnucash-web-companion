# v0.1.6-readonly Release Notes

> Prepared and published in Phase 161 as an authorized read-only maintenance GitHub pre-release.
>
> Publication status: published as a GitHub pre-release. No package, binary artifact, Docker image, or production deployment was created.

## Status

`v0.1.6-readonly` is a conservative pre-alpha read-only maintenance pre-release after the published `v0.1.5-readonly` read-only pre-release and the separately published `v0.2.0-writealpha` experimental pre-release.

The project remains read-only by default. GnuCash Desktop remains the authoritative editor. This project is not production-ready, not security-audited, and not a hosted financial service. Test with disposable fixtures or copied GnuCash SQL books first, keep backups, and do not expose early deployments directly to the public internet.

Controlled-write code may exist in the repository as experimental post-MVP/write-alpha work, but it is outside this read-only maintenance release and remains disabled by default:

```text
GNUCASH_WRITES_ENABLED=false
```

## What changed since v0.1.5-readonly

This release summarizes read-only maintenance, operator UX, localization, and release-candidate dogfood work completed after `v0.1.5-readonly`:

- Phase 153: added a reproducible fresh-clone Docker smoke helper and documented a synthetic/disposable clean-checkout pass with dummy local-only secrets and `GNUCASH_WRITES_ENABLED=false`.
- Phase 154: refreshed GnuCash Desktop compatibility blocker evidence for GitHub #22 without installing packages, opening private books, or claiming Desktop-generated fixture support.
- Phase 155: improved multi-book read-only operator diagnostics with safe storage/access status metadata, private-path redaction, and no upload/delete/default-changing/registry-edit actions.
- Phase 156: added dashboard drilldown links to existing read-only transaction filters while preserving base-currency-only/no-conversion limitations.
- Phase 157: improved scheduled/recurring transaction read-only clarity with URL-only filters, deterministic safe sorting, stronger no-template-split-leak copy, and backend/frontend regression coverage.
- Phase 158: fixed/pinned a narrow-width mobile account/transaction dogfood issue and made browser dogfood default to a 320x720 viewport with no-overflow and CSV export touch-target assertions.
- Phase 159: expanded release-critical English/Russian frontend catalog coverage without claiming full localization.
- Phase 160: reran full synthetic/disposable Docker/Caddy API and browser release-candidate dogfood with `GNUCASH_WRITES_ENABLED=false`; API smoke, browser dogfood, hidden write UI checks, disabled validate/create/patch probes, an additional disabled DELETE probe, mobile no-overflow checks, and no-artifact checks passed.
- Phase 161: re-ran the final local release gate, verified GitHub Actions, and published `v0.1.6-readonly` as an authorized GitHub pre-release.

## Read-only maintenance value

The release is meaningful because it improves the self-hosted read-only operator path and refreshes runtime evidence without expanding write scope:

- a reproducible fresh-clone Docker smoke path for operators;
- safer configured-book status diagnostics without exposing private paths;
- dashboard drilldown links that lead to existing filtered transaction evidence;
- clearer scheduled-transaction metadata and limitations;
- better mobile/touch behavior for read-only account and transaction flows;
- broader release-critical Russian localization coverage;
- full synthetic Docker/Caddy release-candidate API/browser dogfood after the cycle.

## Known limitations and non-claims

This release does not claim:

- production readiness;
- security audit completion;
- safe direct public-internet exposure;
- broad compatibility with every GnuCash Desktop version or every backend;
- PostgreSQL, MySQL, MariaDB, XML-book, or arbitrary real-world-book compatibility;
- hosted SaaS readiness;
- collaborative accounting, family-wallet positioning, or real-time multi-user editing;
- account, transaction, invoice, bill, customer, vendor, scheduled-transaction editing, CSV/OFX import, or banking integrations;
- safe production write mode for real books;
- production scalability or full real-world-book coverage from synthetic dogfood evidence.

Compatibility and dogfood evidence remain intentionally narrow: committed synthetic/disposable SQLite fixtures, copied/disposable metadata procedures, safe local tooling probes, previous redacted copied-book dogfood evidence from earlier releases, fresh-clone smoke evidence, and Phase 160 synthetic Docker/Caddy release-candidate dogfood. Users must test their own copied book before relying on the app for regular read-only browsing.

## Upgrade notes from v0.1.5-readonly

1. Back up app metadata and any copied test books used with the app.
2. Review `.env.example` and keep `GNUCASH_WRITES_ENABLED=false`.
3. Check out the `v0.1.6-readonly` tag from the GitHub pre-release.
4. Rebuild/restart Docker Compose.
5. Run `/api/health`, login, dashboard, accounts, books, scheduled awareness, transactions, account detail, transaction detail, CSV export, and disabled-write checks on disposable or copied data before regular use.
6. Keep `.env`, app DBs, GnuCash books, backups, real financial exports/screenshots, secrets, tokens, keys, and certs out of git.

## Publication status

Published as an authorized GitHub pre-release in Phase 161 after clean `main`, `HEAD == origin/main`, green GitHub CI for the release commit, tag/release absence, local checks, `GNUCASH_WRITES_ENABLED=false`, and sensitive-data hygiene were re-confirmed.

Release URL: https://github.com/valentusys/gnucash-web-companion/releases/tag/v0.1.6-readonly
