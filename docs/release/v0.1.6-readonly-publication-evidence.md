# v0.1.6-readonly Publication Evidence

Date: 2026-05-20
Phase: 161
Release: `v0.1.6-readonly`
Status: published as an authorized GitHub pre-release
Release URL: https://github.com/valentusys/gnucash-web-companion/releases/tag/v0.1.6-readonly

## Decision

Publish `v0.1.6-readonly`.

Phase 160 completed the full synthetic/disposable release-candidate Docker/Caddy API and browser dogfood pass after Phases 153–159. Phase 161 re-ran the final gate and found no blocker: clean `main`, `HEAD == origin/main`, no pre-existing `v0.1.6-readonly` tag/release, green local checks, green GitHub CI for the release commit, rendered `GNUCASH_WRITES_ENABLED=false`, and tracked sensitive-file hygiene all passed.

## Scope published

This is a conservative pre-alpha read-only maintenance pre-release covering Phases 153–160 plus Phase 161 release-gate artifacts:

- reproducible fresh-clone Docker smoke path using synthetic/disposable data;
- GnuCash Desktop compatibility blocker refresh with no broad compatibility claim;
- safer read-only configured-book storage/access diagnostics;
- dashboard drilldowns to existing transaction-filter evidence;
- scheduled/recurring transaction read-only clarity and no-template-leak protections;
- mobile/touch dogfood fix/assertion coverage for account and transaction flows;
- release-critical English/Russian localization coverage;
- full synthetic/disposable Docker/Caddy API/browser release-candidate dogfood;
- release notes/checklist/final-gate/status documentation.

## Pre-publication gate evidence

- Starting Phase 161 HEAD: `d56c335`.
- Starting `origin/main`: `d56c335`.
- Initial working tree: clean tracked `main...origin/main`; untracked `.hermes/` only.
- Initial tag absence: `git tag --list 'v0.1.6-readonly'` returned no tag.
- Initial GitHub release absence: `gh release view v0.1.6-readonly --json tagName,url,isPrerelease,isDraft` returned `release not found`.
- GitHub Actions for starting HEAD `d56c33546647917492b0909a3e23da760642a3c5`: completed successfully before Phase 161 documentation updates.
- Local release checks passed before the Phase 161 release commit:
  - `cd apps/api && pytest -q` — `386 passed, 32 warnings`.
  - `cd apps/web && npm run check` — `0 errors and 0 warnings`.
  - `cd apps/web && npm run test:auth-routes` — passed.
  - `cd apps/web && npm run build` — passed.
  - `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet` — passed.
  - Rendered Compose config kept `GNUCASH_WRITES_ENABLED: "false"` for API and web.
  - `git diff --check` — passed.
  - Sensitive tracked-file hygiene scan — passed.
- GitHub Actions for the Phase 161 release commit were watched to success before creating the tag/release.

## Publication commands

Executed only after the Phase 161 release commit was pushed and CI for that commit passed:

```bash
git tag -a v0.1.6-readonly -m "v0.1.6-readonly"
git push origin v0.1.6-readonly
gh release create v0.1.6-readonly \
  --title "v0.1.6-readonly" \
  --notes-file docs/release/v0.1.6-readonly-notes.md \
  --prerelease
```

## Post-publication checks

- Local tag `v0.1.6-readonly` exists.
- Remote tag `origin` contains `v0.1.6-readonly`.
- `gh release view v0.1.6-readonly --json tagName,url,isPrerelease,isDraft,targetCommitish` reports the expected tag URL and `isPrerelease=true`, `isDraft=false`.
- `main` and `origin/main` match the Phase 161 release commit.
- Working tree is clean except untracked local `.hermes/` agent data.

## Safety boundaries

- `GNUCASH_WRITES_ENABLED=false` remains the documented and rendered default.
- Controlled writes remain post-MVP/write-alpha, experimental, and outside this read-only release scope.
- GnuCash Desktop remains the authoritative editor.
- The release remains pre-alpha, not production-ready, not security-audited, and test/disposable/copy-books-first.
- No package, binary artifact, Docker image, production deployment, product code, endpoint, write route, write-mode UI, runtime default, app DB, GnuCash book, backup, `.env`, token, key, cert, screenshot, CSV/private export, private path, or real/private financial data was added or published.
