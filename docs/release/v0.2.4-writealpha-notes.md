# v0.2.4-writealpha Release Notes

> Status: published as a pre-alpha experimental write-alpha GitHub pre-release.
>
> Publication status: published in Phase 211 after the cycle-1 release gate, green local checks, green GitHub Actions on the exact release/status commit, and tag/release absence checks.
>
> Release URL: https://github.com/valentusys/gnucash-web-companion/releases/tag/v0.2.4-writealpha

## Status

`v0.2.4-writealpha` is a conservative pre-alpha write-alpha maintenance pre-release after `v0.2.3-writealpha`.

This release remains experimental, not production-ready, and not security-audited. Do not expose it directly to the public internet. Do not use write-alpha operations against real/private books or the only copy of a GnuCash book.

The application remains read-only by default:

```text
GNUCASH_WRITES_ENABLED=false
```

When write mode is explicitly enabled for local disposable testing, backend write-alpha routes still require:

```text
APP_ENV=test
```

All new write-alpha evidence for this release is synthetic/disposable fixture evidence only. It is not evidence that real/private-book writes are safe.

## What changed since v0.2.3-writealpha

The release summarizes cycle-1 Phases 202–211 from the 2026-05-20 analyst roadmap:

- Phase 202 — hardened read-only first-run diagnostics for health/login/books error states, with safe handling for missing/default books, placeholder auth configuration, CORS warnings, and disabled writes.
- Phase 203 — advanced the disposable GnuCash Desktop fixture capture path with deterministic provenance checks and an explicit blocker when noninteractive Desktop-generated SQLite fixture creation is unavailable.
- Phase 204 — added compatibility-matrix regression coverage from redacted fixture metadata, separating tested synthetic evidence, blocked/manual Desktop fixture work, and unclaimed backends.
- Phase 205 — polished multi-book read-only recovery for inaccessible, archived, missing, and stale selected-book contexts without adding management/write actions.
- Phase 206 — hardened transaction and scheduled read-only edge cases, including many-split layout, reconciliation-state visibility, no-template redaction, and synthetic Docker/Caddy dogfood.
- Phase 207 — strengthened the read-only write-alpha audit-summary endpoint/UI with bounded counts/status/time-window metadata and redacted rows.
- Phase 208 — polished English/Russian operator safety copy for read-only/write-alpha warnings without claiming full localization.
- Phase 209 — completed full default-read-only Docker/Caddy regression dogfood on the committed synthetic fixture at mobile and desktop widths; validate/create/PATCH/DELETE probes returned 403 and teardown was clean.
- Phase 210 — completed bounded write-alpha create/PATCH/DELETE+restore dogfood on fresh ignored synthetic runtime copies with backup/audit/read-back/lock evidence, default-false reset, and clean teardown.
- Phase 211 — performed this final release gate and publication decision.

## Release scope

The write-alpha surface remains unchanged from earlier write-alpha releases:

- create transaction;
- PATCH transaction metadata/split memo scope as implemented by the write service;
- DELETE transaction.

All write-alpha routes must remain disabled unless both gates are satisfied:

```text
GNUCASH_WRITES_ENABLED=true
APP_ENV=test
```

The default documented and configured posture remains:

```text
GNUCASH_WRITES_ENABLED=false
```

## Safety boundaries and non-claims

This release does not claim:

- production readiness;
- security audit completion;
- safe direct public-internet exposure;
- safe write mode for real/private GnuCash books;
- safe write mode for the only copy of any book;
- broad compatibility with every GnuCash Desktop version or every backend;
- PostgreSQL, MySQL, MariaDB, XML-book, or arbitrary real-world-book compatibility;
- hosted SaaS readiness;
- collaborative accounting, family-wallet positioning, or real-time multi-user editing;
- invoice, bill, customer, vendor, scheduled-transaction editing;
- account editing, recurring transaction editing, CSV/OFX import, or banking integrations.

Use only synthetic/disposable fixtures or copied books that can be restored or deleted. Keep backups. Keep `.env`, app DBs, GnuCash books, backups, real financial exports/screenshots, secrets, tokens, keys, and certs out of git.

## Upgrade notes from v0.2.3-writealpha

1. Treat this as an experimental pre-alpha write-alpha pre-release, not production-ready software.
2. Keep `GNUCASH_WRITES_ENABLED=false` unless intentionally running synthetic/disposable write-alpha tests.
3. Never enable write-alpha routes outside a disposable/test environment; write execution still requires `APP_ENV=test`.
4. Rebuild/restart Docker Compose after updating.
5. Run `/api/health`, login, dashboard, accounts, books, scheduled awareness, transactions, account detail, transaction detail, CSV export, reports summary, audit-summary, and disabled-write checks on disposable or copied data before any regular read-only use.
6. If experimenting with writes, use only synthetic/disposable fixtures or restorable copied test books and follow `docs/dogfood/write-alpha-copied-book-runbook.md`, `docs/write-alpha-maintainer-checklist.md`, and `docs/write-alpha-recovery-procedure.md`.

## Publication status

Published as a GitHub pre-release in Phase 211 after the cycle-1 release gate passed. Publication created only the annotated git tag and GitHub pre-release; no package, binary artifact, Docker image, production deployment, write default change, write-scope expansion, or real/private data artifact was published.
