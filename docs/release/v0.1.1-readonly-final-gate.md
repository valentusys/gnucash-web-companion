# v0.1.1-readonly Final Release Gate

Date: 2026-05-18
Phase: 98
Target release candidate: `v0.1.1-readonly`
Verdict: `Ready for later authorized publish phase`

## Scope

This release gate verifies the prepared `v0.1.1-readonly` read-only maintenance candidate after the Phase 95 CSV export fix, Phase 96 synthetic benchmark confirmation, and Phase 97 draft notes/checklist.

This phase does not publish a git tag, create a GitHub release, upload packages, enable writes, expand controlled-write scope, or claim production readiness.

## Checked revision

| Item | Result |
| --- | --- |
| Branch checked | `main` |
| HEAD checked before recording gate artifact | `e056656e6984a078d3b5fa825670bd105b6df751` |
| Current release target | `v0.1.1-readonly` |
| Gate artifact commit note | The release gate checks below were run before this docs-only gate artifact was committed. No backend, frontend, Docker, auth, or write-mode code was changed by Phase 98. |

## Required checks

| Area | Command / evidence | Result |
| --- | --- | --- |
| Backend test suite | `cd apps/api && pytest -q` | PASS — `329 passed, 27 warnings in 124.13s` |
| Frontend static check | `cd apps/web && npm run check` | PASS — `svelte-check found 0 errors and 0 warnings` |
| Frontend auth-route checks | `cd apps/web && npm run test:auth-routes` | PASS — `auth route checks passed` |
| Frontend production build | `cd apps/web && npm run build` | PASS — SvelteKit/Vite build completed successfully |
| Docker Compose config | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet` | PASS — command exited successfully with no output |
| Whitespace diff check | `git diff --check` | PASS before artifact creation; rerun after docs changes in handoff/final commit verification |
| GitHub auth | `gh auth status` | PASS — authenticated as `valentusys` |
| GitHub Actions recent `main` state | `gh run list --limit 10` | PASS — latest 10 listed CI runs on `main` were `completed/success`, including the Phase 98 PM brief push |
| Tag absence | `git tag --list 'v0.1.1-readonly'` | PASS — no local tag output |
| GitHub release absence | `gh release view v0.1.1-readonly || true` | PASS — `release not found` |
| GitHub #39 | `gh issue view 39 --json number,state,title` | PASS — `CLOSED`, CSV export row-count/header mismatch remains closed |
| GitHub #38 | `gh issue view 38 --json number,state,title` | PASS/known blocker — `OPEN`, copied personal-book dogfood remains blocked pending a safe copied SQL book outside git |

## Read-only and write-disabled safety confirmation

| Check | Result |
| --- | --- |
| Default write posture | PASS — `.env.example`, Docker/default docs, and backend settings retain `GNUCASH_WRITES_ENABLED=false` / `gnucash_writes_enabled: bool = False` as the default posture. |
| Disabled-write coverage | PASS — backend full test suite includes the existing disabled-write route regression coverage; Phase 98 did not enable writes to test the gate. |
| Controlled-write positioning | PASS — controlled writes remain post-MVP/experimental and are not marketed as safe for production books. |
| Product model | PASS — MVP model remains one installation, one local admin user, one default GnuCash book, read-only access. |
| Authoritative editor | PASS — release notes/checklist continue to state that GnuCash Desktop remains the authoritative editor. |

## Sensitive-data hygiene

Safe hygiene evidence only; no secret values or private user directories were inspected.

| Check | Result |
| --- | --- |
| Working tree before implementation | PASS — `git status --short` was clean before Phase 98 docs were created. |
| Changed-file scope | PASS — Phase 98 changes are expected to be release-gate/status/handoff documentation only. |
| Prohibited artifacts | PASS — no real/private financial data, GnuCash books, app DBs, backups, `.env`, secrets, tokens, certs, keys, screenshots, or private CSV exports were created or added for this phase. |
| Auth token handling | PASS — `gh auth status` redacted token output; no token values were copied into committed files. |

## Release state

`v0.1.1-readonly` remains unpublished after this gate:

```text
git tag --list 'v0.1.1-readonly'
(no output)

gh release view v0.1.1-readonly
release not found
```

No tag or GitHub release was created in Phase 98.

## Verdict

`Ready for later authorized publish phase`.

All required Phase 98 release-gate checks passed. The next recommended phase is Phase 99: publish `v0.1.1-readonly` only if the PM/controller explicitly authorizes publication. If publication is authorized, re-confirm tag/release absence immediately before creating the tag/release and keep the release notes conservative: pre-alpha, read-only by default, not production-ready, not security-audited, test disposable/copy books first, do not expose directly to the public internet.

## Remaining risks and non-claims

- GitHub #38 remains open/blocked; no personal copied-book dogfood success is claimed for `v0.1.1-readonly`.
- Compatibility evidence remains intentionally narrow; no broad GnuCash version/backend compatibility claim is made.
- CSV export remains synchronous and capped at 10,000 rows; this release candidate fixes row/header consistency but does not add asynchronous export architecture.
- Controlled writes remain post-MVP/experimental and disabled by default.
- The project remains pre-alpha, not production-ready, and not security-audited.
