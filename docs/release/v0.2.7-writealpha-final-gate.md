# v0.2.7-writealpha Final Gate Draft

Date: 2026-05-21
Phase: 250
Target release: `v0.2.7-writealpha`
Status: `RELEASE CANDIDATE ONLY — NOT PUBLISHED`
Verdict: `CANDIDATE PREPARED — final publication gate deferred to Phase 251`

## Scope

This final-gate draft records the Phase 250 release-candidate decision for Cycle 2 ownership-guard work after `v0.2.6-writealpha`. It reviews whether the repository is ready to ask a later Phase 251 PM/release gate about publishing a conservative write-alpha maintenance pre-release.

Phase 250 does not publish a tag, GitHub release, package, image, or deployment.

## Candidate rationale

| Area | Evidence | Candidate result |
| --- | --- | --- |
| Backend ownership markers | Phase 243 records app metadata-only ownership markers for successful write-alpha CREATE transactions. | PASS |
| PATCH ownership guard | Phase 244 rejects non-owned historical/imported/manual PATCH attempts with 403 before write-service construction, backup, lock, audit row, or GnuCash mutation. | PASS |
| DELETE ownership guard | Phase 245 rejects non-owned historical/imported/manual DELETE attempts with 403 before write-service construction, backup, lock, audit row, or GnuCash mutation. | PASS |
| Frontend alignment | Phase 246 hides edit/delete controls for non-owned transactions and keeps backend guards authoritative. | PASS |
| Synthetic route-family dogfood | Phase 247 created, PATCHed, and DELETEd one write-alpha-owned synthetic transaction, rejected non-owned PATCH/DELETE with 403, recorded redacted backup/audit/lock/restore evidence, and reset to default-disabled writes. | PASS |
| Audit summary ownership view | Phase 248 exposes safe ownership counters without raw payloads, amounts, memos, account names, or paths. | PASS |
| Operator warnings | Phase 249 synchronized docs so operators know historical/imported/manual transactions remain read-only and ownership guards do not make real/private or only-copy books safe. | PASS |

## Phase 250 local verification

| Check | Result |
| --- | --- |
| `python3 scripts/check_public_status.py` | PASS. |
| `cd apps/api && pytest -q` | PASS. |
| `cd apps/web && npm run check && npm run test:auth-routes && npm run build` | PASS. |
| `JWT_SECRET=<dummy-local-secret> APP_ADMIN_PASSWORD=<dummy-local-password> docker compose config --quiet` | PASS. |
| Rendered Compose write default | PASS — API and web keep `GNUCASH_WRITES_ENABLED=false`. |
| Safety greps for write flag, backend flag, `APP_ENV=test`, and browser storage | PASS — no new weakening found. |
| `git diff --check` | PASS. |
| Sensitive tracked-file hygiene scan | PASS. |

## Safety confirmation

- `GNUCASH_WRITES_ENABLED=false` remains default.
- Explicit write-alpha execution remains constrained by `APP_ENV=test` and local enablement.
- Phase 250 changes release/status documentation only and prepares a candidate for a later decision.
- No write endpoint, write scope, write default, or gate behavior changed.
- No create/PATCH/DELETE dogfood mutation was run in this phase.
- No real/private/only-copy book, app DB, backup, `.env`, screenshot/export, token, key, cert, raw path, account name, memo, amount, or private financial data was committed.
- Candidate notes deny production readiness, stable release status, security audit coverage, public-internet safety, broad compatibility, and real/private or only-copy write safety.

## Deferred Phase 251 publication gate

Before any publication, Phase 251 must:

1. Call PM/Project Lead for a release/no-release decision.
2. Re-run the full local release gate.
3. Verify a clean tracked tree and `HEAD == origin/main` for the release/status commit.
4. Verify exact release/status commit GitHub Actions are green.
5. Confirm local tag, remote tag, and GitHub release absence for `v0.2.7-writealpha` immediately before publication.
6. Publish only an annotated git tag and GitHub pre-release if all gates pass and PM authorizes.

No package, Docker image, binary artifact, production deployment, write-default change, `APP_ENV=test` gate weakening, or real/private-book safety claim is authorized by this draft.
