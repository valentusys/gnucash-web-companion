# v0.1.3-readonly Release-Prep Checklist

> Status: prepared in Phase 122 for a possible future `v0.1.3-readonly` maintenance pre-release.
>
> Publication status: unpublished / pending explicit authorization. Phase 122 must not create a git tag, GitHub release, package, or uploaded artifact.

## 1. Candidate scope

Status: prepared, not authorized for publication.

- Release candidate name: `v0.1.3-readonly`.
- Release type: conservative pre-alpha read-only maintenance candidate after published `v0.1.2-readonly`.
- MVP baseline remains: one self-hosted installation, one local admin user, one default configured GnuCash book, read-only access.
- GnuCash Desktop remains the authoritative editor.
- Multi-book wording remains about independent accessible books, not collaborative accounting.
- Controlled-write code remains experimental post-MVP work and disabled by default.
- Required default posture remains:

```text
GNUCASH_WRITES_ENABLED=false
```

## 2. Candidate evidence from Phases 118–122

Meaningful read-only stabilization work exists after `v0.1.2-readonly`:

- Phase 118 — desktop transaction table column-width/horizontal-scroll stabilization and account-tree grid overflow cleanup, with frontend static regression coverage.
- Phase 119 — service-layer account full-name cleanup that removes the synthetic `Root Account:` prefix while preserving account identity/API shape.
- Phase 120 — account-detail read-only transaction retrieval optimization and synthetic benchmark evidence for account-detail list/CSV paths.
- Phase 121 — Conservative dashboard summary zero-value fallback fix using base-currency split-derived asset/liability totals only when stored account balances are zero.
- Phase 122 — read-only stability gate, Docker Compose config validation, tag/release absence checks, sensitive tracked-file hygiene scan, and unpublished release-prep artifacts.

## 3. Prepared artifacts

Prepared in Phase 122:

- `docs/release/v0.1.3-readonly-notes.md` — draft conservative release notes for a later authorized publish phase.
- `docs/release/v0.1.3-readonly-checklist.md` — this release-prep checklist.
- `docs/release/v0.1.3-readonly-final-gate.md` — release-gate verdict and verification evidence.
- `CHANGELOG.md` — records Phase 122 release-prep/gate work without publication.
- `PROJECT_STATUS.md` — advances the baseline through Phase 122 and records that `v0.1.3-readonly` is unpublished.
- `docs/handoff/phase-122.md` — phase handoff and verification report.

## 4. Required checks for Phase 122 gate

Phase 122 records current results for:

```bash
cd apps/api && pytest -q
cd apps/web && npm run check
cd apps/web && npm run test:auth-routes
cd apps/web && npm run build
JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet
git diff --check
git tag --list 'v0.1.3-readonly'
gh release view v0.1.3-readonly || true
gh run list --limit 10 --json status,conclusion,headBranch,displayTitle,url
python - <<'PY'
# tracked-file sensitive hygiene scan; see final gate for exact command
PY
```

Before any later publish phase, re-run the full release-state and safety checks immediately before creating a tag/release.

## 5. Publication steps reserved for a later explicitly authorized phase

Do not run these in Phase 122. They are documented only so a later authorized release executor can copy them after re-checking the gate:

```bash
git tag -a v0.1.3-readonly -m "v0.1.3-readonly"
git push origin v0.1.3-readonly
gh release create v0.1.3-readonly \
  --title "v0.1.3-readonly" \
  --notes-file docs/release/v0.1.3-readonly-notes.md \
  --prerelease
```

Required preconditions before those commands in a future phase:

- explicit Val authorization to publish `v0.1.3-readonly`;
- clean `main` and `HEAD == origin/main`;
- recent GitHub Actions for `main` are successful or any blocker is documented and accepted;
- no existing local tag named `v0.1.3-readonly`;
- no existing GitHub release named `v0.1.3-readonly`;
- final gate still says ready;
- no real/private financial data or secrets in tracked files.

## 6. Safety checklist

Before any future publication, re-confirm:

- `GNUCASH_WRITES_ENABLED=false` remains the default.
- Release notes say pre-alpha and read-only by default.
- Release notes say not production-ready and not security-audited.
- Release notes tell users to test disposable/copy books first and keep backups.
- Release notes do not claim broad GnuCash compatibility.
- Release notes do not claim production-safe write mode.
- Release notes do not overstate Phase 120 synthetic performance evidence.
- No tag/release is created until the publish phase is explicitly authorized.
- No real/private financial data, GnuCash books, `.env`, app DBs, backups, screenshots, exports, secrets, tokens, certs, or keys are committed.

## 7. Known limitations and open risks

- Compatibility evidence remains intentionally narrow; future real Desktop-generated synthetic SQLite fixture coverage is still useful.
- CSV export remains synchronous and capped at 10,000 rows.
- Russian localization remains incomplete and English remains canonical.
- Controlled writes remain post-MVP/experimental and disabled by default; no write-mode feature should be release-marketed as safe for production books.
- Phase 120 performance evidence is local/synthetic only and must not be presented as production scalability proof.
