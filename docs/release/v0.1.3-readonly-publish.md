# v0.1.3-readonly Publish Record

Date: 2026-05-19
Phase: 125
Target: `v0.1.3-readonly`
Status: publication authorized by Val with instruction: "глуши, удаляй, релиз".

## Pre-publish cleanup

- Stopped the temporary local Docker/Caddy live-stand deployment with `docker compose down --remove-orphans`.
- Removed ignored local runtime artifacts for the personal-book stand: `.env`, `data/books/val-personal-readonly.gnucash.sqlite`, and temporary `/tmp/gwc-*`/cookie files.
- Verified no `gnucash-web-companion-*` containers remained running.
- Verified working tree was clean before release-documentation edits.

## Pre-publish checks

- `HEAD == origin/main` before release-doc edits: `44760a1`.
- Local tag absence: `git tag --list 'v0.1.3-readonly'` returned no output.
- GitHub release absence: `gh release view v0.1.3-readonly` returned `release not found`.
- Recent GitHub Actions on `main` were successful through `Harden write-alpha transaction create flow`.
- Docker Compose config validation: `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet` passed.
- `git diff --check` passed.
- Sensitive tracked-file hygiene scan passed; no real/private GnuCash book, app DB, `.env`, backup, screenshot/export, secret, token, key, or cert was tracked.

## Release scope

This remains a conservative pre-alpha/read-only maintenance release. It includes the live-stand read-only fixes from Phases 118–121, the Phase 122 read-only release gate, and disabled-by-default/test-fixture-only write-alpha safety hardening from Phases 123–124.

`GNUCASH_WRITES_ENABLED=false` remains the default. Controlled writes remain experimental, post-MVP, test-fixture-only when enabled, and not a safe production write mode.

## Publication commands

```bash
git tag -a v0.1.3-readonly -m "v0.1.3-readonly"
git push origin v0.1.3-readonly
gh release create v0.1.3-readonly --title "v0.1.3-readonly" --notes-file docs/release/v0.1.3-readonly-notes.md --prerelease
```

## Non-claims

No packages, binaries, real/private GnuCash books, app DBs, backups, screenshots, CSV exports, secrets, tokens, keys, certs, or production write-mode guarantees are published.
