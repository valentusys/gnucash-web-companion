# v0.2.1-writealpha Final Release-Readiness Gate

Date: 2026-05-20
Phase: 181; refreshed for authorized publication in Phase 182
Target release candidate: `v0.2.1-writealpha`
Verdict: `Published as authorized GitHub pre-release after fresh Phase 182 gate`

## Scope

This gate compares the current repository state, release/status documentation, changelog, dogfood evidence, GitHub releases/actions, and safety defaults after the completed Phase 173–180 write-alpha dogfood cycle.

Phase 181 prepared release-readiness artifacts only. Phase 182 re-ran the fresh pre-publish gate after explicit Val authorization, then published only the annotated git tag and GitHub pre-release. It did not upload packages, publish images, enable writes by default, weaken the `APP_ENV=test` write gate, expand write scope, or claim production readiness.

## Checked revision before Phase 181 artifacts

| Item | Result |
| --- | --- |
| Branch checked | `main` |
| HEAD before Phase 181 artifacts | `869879879f0b69cf62ea39714c3f69a2d6c11e11` |
| `origin/main` before Phase 181 artifacts | `869879879f0b69cf62ea39714c3f69a2d6c11e11` |
| Current public read-only release | `v0.1.7-readonly` |
| Current public write-alpha release | `v0.2.0-writealpha` |
| Candidate prepared | `v0.2.1-writealpha` |
| Publication status | Published in Phase 182 as GitHub pre-release |
| Phase 182 release commit | The release/status commit containing `docs/release/v0.2.1-writealpha-publication-evidence.md`; exact SHA is verified by the tag/release after publication. |
| Release URL | https://github.com/valentusys/gnucash-web-companion/releases/tag/v0.2.1-writealpha |

## Documentation/status comparison

| Area | Finding | Result |
| --- | --- | --- |
| `README.md` / `README.ru.md` | Needed synchronization beyond Phase 172 because Phases 173–180 added write-alpha dogfood evidence and hardening. | Updated in Phase 181. |
| `CHANGELOG.md` | `Unreleased` previously recorded Phase 172 only. | Updated with Phases 173–181 and unpublished `v0.2.1-writealpha` candidate status. |
| Existing `v0.2.0-writealpha` docs | Correctly remain historical/published and do not claim Phase 173–180 evidence. | Kept intact; new candidate docs are separate. |
| Dogfood evidence | Phase 175–180 docs show copied/disposable or synthetic-only write-alpha evidence and default read-only regression. | Accepted as candidate evidence with limits. |
| Safety wording | Docs must not promote write-alpha to real/private-book-safe. | Preserved: candidate notes/checklist explicitly deny production/security/real-book write-safety claims. |

## Required checks

| Area | Command / evidence | Result |
| --- | --- | --- |
| Working tree before implementation | `git status --short` | PASS — only untracked `.hermes/` was present and excluded. |
| HEAD/origin parity before implementation | `git rev-parse HEAD origin/main` | PASS — both `869879879f0b69cf62ea39714c3f69a2d6c11e11`. |
| Recent releases | `gh release list --limit 10` | PASS — latest releases include `v0.1.7-readonly` and existing `v0.2.0-writealpha`; no `v0.2.1-writealpha` release existed. |
| Recent GitHub Actions before implementation | `gh run list --limit 10` | PASS — latest ten `main` CI runs were completed/success, including Phase 180. |
| Docker Compose config | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet` | PASS — rendered config is valid. |
| Write-disabled default | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config | grep -n 'GNUCASH_WRITES_ENABLED'` | PASS — API and web both render `GNUCASH_WRITES_ENABLED: "false"`. |
| Backend suite | `cd apps/api && pytest -q` | PASS — `404 passed, 33 warnings in 156.90s`. |
| Frontend static check | `cd apps/web && npm run check` | PASS — `svelte-check found 0 errors and 0 warnings`. |
| Frontend auth-route checks | `cd apps/web && npm run test:auth-routes` | PASS — `auth route checks passed`. |
| Frontend production build | `cd apps/web && npm run build` | PASS — SvelteKit/Vite build completed successfully. |
| Whitespace diff check | `git diff --check` | PASS in Phase 181 verification. |
| Sensitive tracked-file scan | Python scan over `git ls-files` with known synthetic/docs allowlist | PASS in Phase 181 verification. |

## Phase 182 fresh pre-publish gate

| Area | Command / evidence | Result |
| --- | --- | --- |
| Working tree at gate start | `git status --short` | PASS — only untracked `.hermes/` was present and excluded. |
| HEAD/origin parity | `git fetch origin main --tags --prune`; `git rev-parse HEAD origin/main` | PASS — both `ee134ea221db89a83388ba64967a8c3a41fd4c2b`. |
| Local tag absence | `git tag -l v0.2.1-writealpha` | PASS — no local tag before publication. |
| Remote tag absence | `git ls-remote --tags origin refs/tags/v0.2.1-writealpha` | PASS — no remote tag before publication. |
| GitHub release absence | `gh release view v0.2.1-writealpha --json tagName,url,isPrerelease,isDraft,targetCommitish` | PASS — `release not found` before publication. |
| GitHub auth | `gh auth status` | PASS — authenticated as `valentusys` with repo/workflow scopes. |
| GitHub Actions on exact commit | `gh run watch <release-commit-run> --exit-status` | PASS — CI completed successfully for the exact Phase 182 release/status commit before publication. |
| Docker Compose config | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet` | PASS. |
| Write-disabled rendered config | `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config \| grep -n 'GNUCASH_WRITES_ENABLED'` | PASS — API and web both rendered `GNUCASH_WRITES_ENABLED: "false"`. |
| `.env.example` default | Search for `GNUCASH_WRITES_ENABLED` in `.env.example` | PASS — `GNUCASH_WRITES_ENABLED=false`. |
| Backend suite | `cd apps/api && pytest -q` | PASS — 404 tests passed with existing warnings. |
| Frontend static check | `cd apps/web && npm run check` | PASS — 0 errors / 0 warnings. |
| Frontend auth-route checks | `cd apps/web && npm run test:auth-routes` | PASS — auth route checks passed. |
| Frontend production build | `cd apps/web && npm run build` | PASS. |
| Whitespace diff check | `git diff --check` | PASS before release-doc commit. |
| Sensitive tracked-file scan | Python scan over `git ls-files` with known synthetic/docs allowlist | PASS before release-doc commit. |

## Recent GitHub release/action state

`gh release list --limit 10` at gate start showed existing published pre-releases through `v0.1.7-readonly` and `v0.2.0-writealpha`; there was no `v0.2.1-writealpha` entry.

`gh run list --limit 10` at gate start showed the latest ten `main` CI runs completed successfully, including the Phase 180 combined regression dogfood commit. A later publish phase must still wait for GitHub Actions on its exact release commit before tagging.

## Write-alpha safety confirmation

| Check | Result |
| --- | --- |
| Default write posture | PASS — `GNUCASH_WRITES_ENABLED=false` remains the default and rendered Compose value. |
| Test-environment write gate | PASS — candidate docs retain `APP_ENV=test` as required for write execution when explicitly enabled. |
| Write scope | PASS — candidate does not add create/PATCH/DELETE endpoints or broaden write behavior. |
| Evidence scope | PASS — Phase 175–180 write evidence is synthetic/disposable or copied-test-book evidence only. |
| Product model | PASS — GnuCash Desktop remains the authoritative editor; the web app remains read-only by default. |
| Non-claims | PASS — candidate notes deny production readiness, security audit, broad compatibility, and real/private/only-copy write safety. |

## Sensitive-data hygiene

No private or real financial data was needed for this gate. Phase 181 changes are release/status/handoff documentation only.

Prohibited artifacts remain excluded: real/private GnuCash books, app DBs, backups, `.env`, secrets, tokens, certs, keys, screenshots, CSV/private exports, package uploads, and private media.

## Publication decision

Decision: publish `v0.2.1-writealpha` as an authorized GitHub pre-release after the Phase 182 fresh gate.

Rationale:

- A maintenance write-alpha pre-release is warranted because Phases 173–180 add meaningful copied/disposable dogfood evidence plus UX/API hardening after published `v0.2.0-writealpha`.
- The Phase 182 fresh gate was locally green, GitHub Actions for the exact release commit passed, tag/release absence was confirmed, and safety defaults remained intact.
- The Phase 182 contract explicitly authorized publication if the gate was green.

## Publication result

Phase 182 published only:

- annotated git tag `v0.2.1-writealpha` on the release/status commit;
- GitHub pre-release `v0.2.1-writealpha` from `docs/release/v0.2.1-writealpha-notes.md`.

No package, binary artifact, Docker image, production deployment, write-default change, write-scope expansion, real/private book, app DB, backup, `.env`, screenshot/export, token, key, cert, or private financial artifact was published.

## Remaining risks and non-claims

- Publication completed in Phase 182 after explicit Val authorization and a fresh pre-publish gate.
- Write-alpha remains experimental, disabled by default, and executable only with `APP_ENV=test` when explicitly enabled.
- Real/private-book write safety is not established and not claimed.
- Compatibility evidence remains intentionally narrow; no broad GnuCash version/backend compatibility claim is made.
- CSV export remains synchronous and capped at 10,000 rows.
- The project remains pre-alpha, not production-ready, and not security-audited.
