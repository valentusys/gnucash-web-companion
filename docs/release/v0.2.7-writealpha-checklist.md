# v0.2.7-writealpha Release Checklist

Date: 2026-05-21
Phase: 251
Target release: `v0.2.7-writealpha`
Status: `PUBLISHED AS GITHUB PRE-RELEASE`
Result: `PASS — publish as GitHub pre-release`

## Checklist

| Check | Result | Evidence |
| --- | --- | --- |
| Candidate is a conservative pre-alpha write-alpha maintenance release | PASS | Notes/checklist/final-gate keep pre-alpha, default-disabled, `APP_ENV=test`-gated, synthetic/disposable-only language. |
| Backend ownership guards are implemented | PASS | Phase 244 PATCH guard and Phase 245 DELETE guard require same-book app metadata ownership before constructing the write service for enabled PATCH/DELETE. |
| Synthetic route-family dogfood passed | PASS | Phase 247 created, PATCHed, and DELETEd one write-alpha-owned synthetic transaction; non-owned PATCH/DELETE probes returned 403 without backup growth; evidence was redacted. |
| Ownership docs are synchronized | PASS | Phase 249 updated write-alpha ownership docs, copied-book dogfood runbook, environment guidance, and dogfood evidence schema. |
| PM authorization for publication | PASS | Phase 251 Project Lead decision: `AUTHORIZE_RELEASE`. |
| Candidate local tag absent before publication | PASS | `git tag -l v0.2.7-writealpha` returned no tag before release publication. |
| Candidate remote tag absent before publication | PASS | `git ls-remote --tags origin refs/tags/v0.2.7-writealpha refs/tags/v0.2.7-writealpha^{}` returned no tag before release publication. |
| Candidate GitHub release absent before publication | PASS | `gh release view v0.2.7-writealpha` returned not found before release publication. |
| `.env.example` default keeps writes disabled | PASS | `GNUCASH_WRITES_ENABLED=false`. |
| Rendered Docker Compose keeps writes disabled | PASS | Final gate renders default false for API and web. |
| Backend write-alpha remains test-gated | PASS | `APP_ENV=test` gate unchanged; no write-default enablement. |
| Release notes deny real/private-book safety | PASS | Notes say evidence is synthetic/disposable only and no real/private or only-copy write safety is claimed. |
| Release notes deny production/security claims | PASS | Notes deny production readiness, stable release status, security audit, public-internet safety, hosted SaaS readiness, and broad compatibility. |
| Backend tests | PASS | `cd apps/api && pytest -q`. |
| Frontend checks/build | PASS | `cd apps/web && npm run check && npm run test:auth-routes && npm run build`. |
| Docker Compose config | PASS | `JWT_SECRET=<dummy-local-secret> APP_ADMIN_PASSWORD=<dummy-local-password> docker compose config --quiet`. |
| Public status guard | PASS | `python3 scripts/check_public_status.py`. |
| Safety greps | PASS | Write default, backend flag, `APP_ENV=test` wording, and browser-storage checks found no weakening. |
| Whitespace diff | PASS | `git diff --check`. |
| Sensitive tracked-file hygiene | PASS | tracked-file scan passed. |
| Exact release/status commit CI | PASS | GitHub Actions were watched to success on the pushed release/status commit before tag/release publication. |

## Decision

Publish `v0.2.7-writealpha` as a GitHub pre-release after the final local gate, sensitive tracked-file hygiene, exact release/status commit CI, and tag/release absence checks passed.

The release is justified because Cycle 2 produced meaningful ownership-boundary safety work: backend PATCH/DELETE ownership guards, aligned frontend controls, synthetic ownership route-family dogfood, safe ownership counters in the audit summary, and synchronized operator warnings.

## Publication scope

Published only:

- annotated git tag `v0.2.7-writealpha`;
- GitHub pre-release `v0.2.7-writealpha` using `docs/release/v0.2.7-writealpha-notes.md`.

No package, binary artifact, Docker image, production deployment, write-default change, write-scope expansion, real/private-book safety claim, or security/production claim is included.
