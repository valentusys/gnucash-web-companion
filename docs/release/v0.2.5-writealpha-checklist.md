# v0.2.5-writealpha Release Checklist — no-release result

Date: 2026-05-21
Phase: 221
Target candidate: `v0.2.5-writealpha`
Result: `FAIL — no release published`

## Release-prep checklist

| Check | Result | Evidence |
| --- | --- | --- |
| Current branch is `main` and starts equal to `origin/main` | PASS | `HEAD == origin/main` at `f2b2fd6b3160cef7518b6ce270ef26bd9e1c088c` before Phase 221 docs. |
| Candidate local tag absent | PASS | `git tag -l v0.2.5-writealpha` returned none. |
| Candidate remote tag absent | PASS | `git ls-remote --tags origin refs/tags/v0.2.5-writealpha` returned none. |
| Candidate GitHub release absent | PASS | `gh release view v0.2.5-writealpha` returned `release not found`. |
| GitHub auth available | PASS | `gh auth status` succeeded for `valentusys`. |
| README/README.ru/PROJECT_STATUS/CHANGELOG/docs/release/docs/dogfood compared | PASS | Phase 220 dogfood explicitly names the DELETE backup-count anomaly as a no-release blocker. |
| `.env.example` default keeps writes disabled | PASS | `GNUCASH_WRITES_ENABLED=false`. |
| Rendered Docker Compose keeps writes disabled | PASS | API and web rendered `GNUCASH_WRITES_ENABLED: "false"`. |
| Backend write-alpha remains test-gated | PASS | No `APP_ENV=test` gate change in this phase. |
| Backend tests | PASS | `cd apps/api && pytest -q` passed (`521 passed`). |
| Frontend checks/build | PASS | `npm run check`, `npm run test:auth-routes`, and `npm run build` passed. |
| Docker Compose config | PASS | `JWT_SECRET=<dummy-local-secret> APP_ADMIN_PASSWORD=<dummy-local-password> docker compose config --quiet` passed. |
| Whitespace diff | PASS | `git diff --check` passed. |
| Sensitive tracked-file hygiene | PASS | tracked-file scan passed. |
| Release-candidate dogfood evidence | FAIL | Phase 220 DELETE backup-count anomaly after successful synthetic/disposable DELETE: successful backup-bearing audit rows exceeded backup file count. |

## Decision

Do not publish `v0.2.5-writealpha`.

The release gate fails even though local standard checks and default-read-only evidence are green, because the cycle-2 write-alpha release requires bounded create/PATCH/DELETE evidence and Phase 220 found a DELETE backup-count mismatch.

Phase 229 refreshed this release-support posture after Phases 222–228 remediated and smoke-verified the blocker closure in synthetic/disposable scope. The current published write-alpha pre-release remains `v0.2.4-writealpha` until a later authorized release phase succeeds.

## Non-publication controls

- No annotated tag was created.
- No GitHub pre-release was created.
- No package, binary artifact, Docker image, or production deployment was published.
- `GNUCASH_WRITES_ENABLED=false` remains default.
- Write-alpha remains experimental, local-test-gated, and synthetic/disposable-evidence-only.
