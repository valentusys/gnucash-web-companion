# v0.2.5-writealpha Release Checklist

Date: 2026-05-21
Phase: 231
Release: `v0.2.5-writealpha`
Result: `PASS — publish as GitHub pre-release after exact release-commit CI`

## Release checklist

| Check | Result | Evidence |
| --- | --- | --- |
| Current branch is `main` and starts equal to `origin/main` | PASS | Starting HEAD and `origin/main`: `22be298db4e81be4a07529cc4e65d67234f6b5b6`; tracked tree clean except untracked `.hermes/`. |
| Previous Phase 230 CI checked | PASS | Phase 230 `main` CI was observed before the Phase 231 release/status push; the exact Phase 231 release/status commit CI is the publication gate. |
| Candidate local tag absent before publication | PASS | `git tag -l v0.2.5-writealpha` returned none. |
| Candidate remote tag absent before publication | PASS | `git ls-remote --tags origin refs/tags/v0.2.5-writealpha` returned none. |
| Candidate GitHub release absent before publication | PASS | `gh release view v0.2.5-writealpha` returned `release not found`. |
| GitHub auth available | PASS | `gh auth status` succeeded for `valentusys`. |
| Phases 222–230 evidence reviewed | PASS | Blocker remediation, backup identity, DELETE restore proof, create/PATCH/DELETE matrix, default read-only regression, blocker-closure wording, fresh-clone/upgrade smoke, public drift guard, and Phase 230 final dogfood pack are green in docs/handoff and docs/dogfood/release artifacts. |
| `.env.example` default keeps writes disabled | PASS | `GNUCASH_WRITES_ENABLED=false`. |
| Rendered Docker Compose keeps writes disabled | PASS | API and web render `GNUCASH_WRITES_ENABLED: "false"` with dummy local secrets. |
| Backend write-alpha remains test-gated | PASS | `APP_ENV=test` gate unchanged; no write-default enablement. |
| Backend tests | PASS | `cd apps/api && pytest -q`. |
| Frontend checks/build | PASS | `npm run check`, `npm run test:auth-routes`, and `npm run build`. |
| Docker Compose config | PASS | `JWT_SECRET=<dummy-local-secret> APP_ADMIN_PASSWORD=<dummy-local-password> docker compose config --quiet`. |
| Public status guard | PASS | `python3 scripts/check_public_status.py`. |
| Whitespace diff | PASS | `git diff --check`. |
| Sensitive tracked-file hygiene | PASS | tracked-file scan passed. |
| Release notes safety wording | PASS | Notes deny production readiness, security audit, stable status, public-internet safety, broad compatibility, and real/private/only-copy write safety. |
| Exact release/status commit CI | PASS before publication | GitHub Actions must pass on the pushed Phase 231 release/status commit before annotated tag and GitHub pre-release creation. |

## Decision

Publish `v0.2.5-writealpha` only after the Phase 231 release/status commit is pushed and its exact GitHub Actions run passes.

Publication scope is limited to:

- annotated git tag `v0.2.5-writealpha`;
- GitHub pre-release `v0.2.5-writealpha` using `docs/release/v0.2.5-writealpha-notes.md`.

No package, binary artifact, Docker image, production deployment, write-default change, write-scope expansion, real/private-book safety claim, or security/production claim is authorized.
