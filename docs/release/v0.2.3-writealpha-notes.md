# v0.2.3-writealpha Release Notes

> Status: published as a pre-alpha experimental write-alpha GitHub pre-release.
>
> Publication status: published in Phase 201 after the cycle-3 release-readiness gate, green local checks, green GitHub Actions on the exact release/status commit, and tag/release absence checks.
>
> Release URL: https://github.com/valentusys/gnucash-web-companion/releases/tag/v0.2.3-writealpha

## Status

`v0.2.3-writealpha` is a conservative pre-alpha write-alpha maintenance pre-release after `v0.2.2-writealpha`.

This release remains experimental, not production-ready, and not security-audited. Do not expose it directly to the public internet. Do not use write-alpha operations against real/private books or the only copy of a GnuCash book.

The application remains read-only by default:

```text
GNUCASH_WRITES_ENABLED=false
```

When write mode is explicitly enabled for local disposable testing, backend write-alpha routes still require:

```text
APP_ENV=test
```

All new write-alpha evidence for this release is synthetic/disposable fixture evidence only. It is not evidence that real/private-book writes are safe.

## What changed since v0.2.2-writealpha

The release summarizes cycle-3 Phases 192–201:

- Phase 192 — moved GitHub Actions JavaScript actions to Node 24-compatible major versions, removing the known non-blocking Node.js 20 action deprecation warnings without changing product behavior or runtime defaults.
- Phase 193 — added a stopped-runtime-only, path-redacted cleanup helper for ignored runtime files and stale/unreadable locks, including root-owned artifact handling through Docker Compose when needed.
- Phase 194 — made write-alpha create/PATCH/DELETE smoke helpers resilient to root-owned host-side runtime artifacts by collecting redacted backup/audit/lock evidence inside the API container without rerunning mutating routes.
- Phase 195 — hardened the read-only write-alpha audit-summary operator UX with safe action/result/time-window filters and bounded redacted counts/status metadata.
- Phase 196 — added redacted first-run/read-only deployment diagnostics for JWT/admin/default-book/CORS/write-mode triage through `/health` and `/login`.
- Phase 197 — refreshed GnuCash Desktop fixture compatibility blocker evidence with a disposable GnuCash 4.13 container probe and redacted metadata/provenance collector tests; no broad Desktop support is claimed.
- Phase 198 — hardened multi-book read-only registry diagnostics, role/status copy, unavailable-book actions, and selected-book recovery without exposing raw paths or adding registry/write management.
- Phase 199 — completed full default-read-only Docker/Caddy regression dogfood on the committed synthetic fixture at mobile and desktop widths; validate/create/PATCH/DELETE probes returned 403 and teardown was clean.
- Phase 200 — completed bounded write-alpha create/PATCH/DELETE disposable route-family dogfood on fresh ignored synthetic runtime copies; backup/audit evidence was present, DELETE restore proof completed, locks were stale-released rather than active, and the stack returned to default false.
- Phase 201 — performed this final release-readiness gate and publication decision.

## Release scope

The write-alpha surface remains unchanged from earlier write-alpha releases:

- create transaction;
- PATCH transaction metadata/split memo scope as implemented by the write service;
- DELETE transaction.

All write-alpha routes must remain disabled unless both gates are satisfied:

```text
GNUCASH_WRITES_ENABLED=true
APP_ENV=test
```

The default documented and configured posture remains:

```text
GNUCASH_WRITES_ENABLED=false
```

## Safety boundaries and non-claims

This release does not claim:

- production readiness;
- security audit completion;
- safe direct public-internet exposure;
- safe write mode for real/private GnuCash books;
- safe write mode for the only copy of any book;
- broad compatibility with every GnuCash Desktop version or every backend;
- PostgreSQL, MySQL, MariaDB, XML-book, or arbitrary real-world-book compatibility;
- hosted SaaS readiness;
- collaborative accounting, family-wallet positioning, or real-time multi-user editing;
- invoice, bill, customer, vendor, scheduled-transaction editing;
- account editing, recurring transaction editing, CSV/OFX import, or banking integrations.

Use only synthetic/disposable fixtures or copied books that can be restored or deleted. Keep backups. Keep `.env`, app DBs, GnuCash books, backups, real financial exports/screenshots, secrets, tokens, keys, and certs out of git.

## Upgrade notes from v0.2.2-writealpha

1. Treat this as an experimental pre-alpha write-alpha pre-release, not production-ready software.
2. Keep `GNUCASH_WRITES_ENABLED=false` unless intentionally running synthetic/disposable write-alpha tests.
3. Never enable write-alpha routes outside a disposable/test environment; write execution still requires `APP_ENV=test`.
4. Rebuild/restart Docker Compose after updating.
5. Run `/api/health`, login, dashboard, accounts, books, scheduled awareness, transactions, account detail, transaction detail, CSV export, reports summary, and disabled-write checks on disposable or copied data before any regular read-only use.
6. If experimenting with writes, use only synthetic/disposable fixtures or restorable copied test books and follow `docs/dogfood/write-alpha-copied-book-runbook.md`, `docs/write-alpha-maintainer-checklist.md`, and `docs/write-alpha-recovery-procedure.md`.

## Publication status

Published as a GitHub pre-release in Phase 201 after the cycle-3 release-readiness gate passed. Publication created only the annotated git tag and GitHub pre-release; no package, binary artifact, Docker image, production deployment, write default change, write-scope expansion, or real/private data artifact was published.
