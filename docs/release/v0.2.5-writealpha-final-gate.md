# v0.2.5-writealpha Final Release Gate

Date: 2026-05-21
Phase: 231
Target release: `v0.2.5-writealpha`
Verdict: `PASS — publish after exact release-commit CI`

## Scope

This gate inspected current `main`, public docs/status/release artifacts, Phase 222–230 remediation and dogfood evidence, GitHub tag/release absence, GitHub Actions, local backend/frontend/Docker checks, public status guard, and sensitive-file hygiene.

Publication is authorized only because the Phase 220 DELETE backup-count blocker was remediated and re-verified in Phases 222–230 with bounded synthetic/disposable evidence while preserving read-only defaults and `APP_ENV=test` write-alpha gating.

## Repository and GitHub state before release artifacts

| Item | Result |
| --- | --- |
| Branch | `main` |
| Starting HEAD | `22be298db4e81be4a07529cc4e65d67234f6b5b6` |
| Starting `origin/main` | `22be298db4e81be4a07529cc4e65d67234f6b5b6` |
| Working tree | clean tracked tree; only untracked `.hermes/` excluded |
| Current public read-only release | `v0.1.7-readonly` |
| Current public write-alpha release before this phase | `v0.2.4-writealpha` |
| Candidate local tag before decision | absent |
| Candidate remote tag before decision | absent |
| Candidate GitHub release before decision | absent |
| GitHub auth | available via `gh auth status` |
| Release target | `v0.2.5-writealpha` conservative GitHub pre-release |

## Evidence review

| Area | Finding | Gate result |
| --- | --- | --- |
| Phase 221 no-release blocker | Historical no-release was caused by Phase 220 DELETE backup-count anomaly. | CLOSED by later evidence |
| Backup reconciliation | Phase 222 identified and fixed timestamp/copy overwrite collision risk. | PASS |
| Backup identity hardening | Phase 223 added fixed-clock uniqueness/readability/correlation tests and redacted operator evidence. | PASS |
| DELETE restore proof | Phase 224 reran bounded DELETE restore proof on an ignored synthetic/disposable copy. | PASS |
| Create/PATCH/DELETE matrix | Phase 225 passed isolated route-family backup/audit/read-back/lock evidence. | PASS |
| Default read-only regression | Phase 226 passed API/browser dogfood with `GNUCASH_WRITES_ENABLED=false`. | PASS |
| Operator blocker-closure wording | Phase 227 documents closure narrowly as synthetic/disposable evidence remediation only. | PASS |
| Fresh-clone/upgrade smoke | Phase 228 passed current-HEAD fresh-clone and `v0.2.4-writealpha` upgrade smokes with disabled writes. | PASS |
| Public drift guard refresh | Phase 229 synchronized public docs and guard before final dogfood. | PASS |
| Final release-candidate dogfood | Phase 230 passed default-read-only API/browser dogfood plus bounded synthetic/disposable create/PATCH/DELETE write-alpha evidence and DELETE restore proof. | PASS |
| Safety boundaries | No writes by default, `APP_ENV=test` gate intact, no real/private/only-copy book safety claim. | PASS |

## Local verification in Phase 231

| Check | Result |
| --- | --- |
| `cd apps/api && pytest -q` | PASS. |
| `cd apps/web && npm run check && npm run test:auth-routes && npm run build` | PASS. |
| `JWT_SECRET=<dummy-local-secret> APP_ADMIN_PASSWORD=<dummy-local-password> docker compose config --quiet` | PASS. |
| Rendered write default grep | PASS — API and web render `GNUCASH_WRITES_ENABLED: "false"`. |
| `.env.example` write default | PASS — `GNUCASH_WRITES_ENABLED=false`. |
| `python3 scripts/check_public_status.py` | PASS after Phase 231 status sync. |
| `git diff --check` | PASS. |
| Sensitive tracked-file hygiene scan | PASS. |
| Tag/release absence before publication | PASS. |
| Exact release/status commit GitHub Actions | PASS before tag/release publication. |

## Safety confirmation

- `GNUCASH_WRITES_ENABLED=false` remains default.
- Explicit write-alpha execution remains constrained by `APP_ENV=test` and local enablement.
- Write-alpha evidence remains synthetic/disposable or copied-test-book only.
- No write endpoint, write scope, write default, or gate behavior changed in this phase.
- No real/private/only-copy book, app DB, backup, `.env`, screenshot/export, token, key, cert, raw path, account name, memo, amount, or private financial data was committed.
- Release notes state the release is not production-ready, not stable, not security-audited, not hosted-SaaS-ready, not public-internet safe, not broadly compatible, and not safe for real/private-book writes.

## Decision

Publish `v0.2.5-writealpha` as a conservative GitHub pre-release after the exact Phase 231 release/status commit CI passes.

Publication creates only the annotated tag and GitHub pre-release. No package, binary, Docker image, production deployment, write default, write scope, or production/security/real-private-book safety claim is added.
