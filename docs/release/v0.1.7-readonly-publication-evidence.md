# v0.1.7-readonly Publication Evidence

Date: 2026-05-20
Phase: 171
Release: `v0.1.7-readonly`
Status: pending publication after release commit CI passes
Release URL after publication: https://github.com/valentusys/gnucash-web-companion/releases/tag/v0.1.7-readonly

## Decision

Publish `v0.1.7-readonly` only if the Phase 171 release commit CI passes and the final pre-tag checks still show clean `main`, `HEAD == origin/main`, absent tag/release, write-disabled defaults, and sensitive-data hygiene.

Phase 170 completed the full cycle 2 synthetic/disposable Docker/Caddy API and browser dogfood pass after Phases 162–169. Phase 171 re-ran the final local gate and found no local blocker: clean `main`, `HEAD == origin/main`, no pre-existing `v0.1.7-readonly` local tag or GitHub release, green local checks, rendered `GNUCASH_WRITES_ENABLED=false`, and tracked sensitive-file hygiene all passed.

## Scope to publish

This is a conservative pre-alpha read-only maintenance pre-release covering Phases 162–170 plus Phase 171 release-gate artifacts:

- post-`v0.1.6-readonly` baseline sync and fresh-checkout tagged smoke;
- disposable-container GnuCash Desktop/CLI tooling probe with no broad compatibility claim;
- selected-book cookie/access edge-case recovery hardening;
- large account-tree synthetic benchmark and deep-hierarchy UI overflow guard;
- CSV export feedback and account-scoped parity hardening;
- local/LAN auth/session origin and cookie-expiry hardening;
- first-run/broken-configuration operator diagnostics;
- release-critical Russian localization slice;
- full synthetic/disposable Docker/Caddy API/browser release-candidate dogfood;
- release notes/checklist/final-gate/status documentation.

## Pre-publication gate evidence

- Starting Phase 171 HEAD: `cec8e5e`.
- Starting `origin/main`: `cec8e5e`.
- Initial working tree: clean tracked `main...origin/main`; untracked `.hermes/` only.
- Initial tag absence: `git tag --list 'v0.1.7-readonly'` returned no tag.
- Initial GitHub release absence: `gh release view v0.1.7-readonly --json tagName,url,isPrerelease,isDraft` returned `release not found`.
- GitHub Actions for starting HEAD `cec8e5ed07d83335efcd5af72be89494ff932c0f`: completed successfully before Phase 171 documentation updates.
- Local release checks passed before the Phase 171 release commit:
  - `cd apps/api && pytest -q` — `395 passed, 32 warnings`.
  - `cd apps/web && npm run check` — `0 errors and 0 warnings`.
  - `cd apps/web && npm run test:auth-routes` — passed.
  - `cd apps/web && npm run build` — passed.
  - `JWT_SECRET=dummy-validation-secret APP_ADMIN_PASSWORD=dummy docker compose config --quiet` — passed.
  - Rendered Compose config kept `GNUCASH_WRITES_ENABLED: "false"` for API and web.
  - `git diff --check` — passed before artifact edits.
  - Sensitive tracked-file hygiene scan — passed.
- GitHub Actions for the Phase 171 release commit must be watched to success before creating the tag/release.

## Publication commands

To be executed only after the Phase 171 release commit is pushed and CI for that commit passes:

```bash
git tag -a v0.1.7-readonly -m "v0.1.7-readonly"
git push origin v0.1.7-readonly
gh release create v0.1.7-readonly \
  --title "v0.1.7-readonly" \
  --notes-file docs/release/v0.1.7-readonly-notes.md \
  --prerelease
```

## Post-publication checks

Run after publication:

- Local tag `v0.1.7-readonly` exists.
- Remote tag `origin` contains `v0.1.7-readonly`.
- `gh release view v0.1.7-readonly --json tagName,url,isPrerelease,isDraft,targetCommitish` reports the expected tag URL and `isPrerelease=true`, `isDraft=false`.
- The tag resolves to the verified Phase 171 release commit.
- `main` and `origin/main` match the Phase 171 release commit.
- Working tree is clean except untracked local `.hermes/` agent data.

## Safety boundaries

- `GNUCASH_WRITES_ENABLED=false` remains the documented and rendered default.
- Controlled writes remain post-MVP/write-alpha, experimental, and outside this read-only release scope.
- GnuCash Desktop remains the authoritative editor.
- The release remains pre-alpha, not production-ready, not security-audited, and test/disposable/copy-books-first.
- No package, binary artifact, Docker image, production deployment, product code, endpoint, write route, write-mode UI, runtime default, app DB, GnuCash book, backup, `.env`, token, key, cert, screenshot, CSV/private export, private path, or real/private financial data is included.
