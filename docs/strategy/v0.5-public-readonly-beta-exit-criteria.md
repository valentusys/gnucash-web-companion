# v0.5.0-public-readonly-beta exit criteria

Release type: conservative GitHub pre-release only.

Definition of Done:
- public read-only install/upgrade docs are concise and tested;
- fresh-clone read-only smoke on synthetic/disposable data passes;
- Docker Compose renders with `GNUCASH_WRITES_ENABLED=false`;
- security/privacy posture warns against direct public-internet exposure and no security audit;
- feedback packet asks for no private data, screenshots, exports, amounts, account names, or paths;
- public write mode remains out of scope.

Current Phase 480 status: not met; no v0.5 release.
