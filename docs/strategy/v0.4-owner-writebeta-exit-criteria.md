# v0.4.0-owner-writebeta exit criteria

Release type: conservative GitHub pre-release only.

Definition of Done:
- integrated owner write session workflow exists: preflight, arm, backup, preview, confirm, mutate, read-back, compatibility, restore-ready, reset, redacted audit summary;
- copied/restorable outside-git session evidence exists for PM-authorized operations;
- default `GNUCASH_WRITES_ENABLED=false` and stronger-or-equal `APP_ENV=test` gate are preserved;
- real working-book write, if ever considered, has separate exact owner confirmation and PM authorization;
- no public write, production, security-audited, public-internet, broad compatibility, or only-copy safety claim.

Current Phase 480 status: not met; no v0.4 release.
